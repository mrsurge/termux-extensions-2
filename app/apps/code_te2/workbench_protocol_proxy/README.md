# workbench_protocol_proxy

This directory contains shared decoder helpers used while developing the active
Node Workbench Adapter.

The old Python transparent HTTP+WebSocket reverse proxy has been removed. Active
Code TE2 runtime starts the Node adapter directly via `shellspec/workbench_adapter.yaml`.
