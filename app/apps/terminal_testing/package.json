{
  "private": true,
  "type": "module",
  "name": "terminal-testing",
  "description": "TE2 terminal_testing frontend and broker prototype",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "node-pty": "^1.1.0",
    "reconnecting-websocket": "^4.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "typescript": "^5.7.0"
  }
}
