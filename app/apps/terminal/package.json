{
  "private": true,
  "type": "module",
  "name": "terminal",
  "description": "TE2 terminal frontend and stream transport",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@xterm/addon-web-fonts": "0.1.0",
    "node-pty": "^1.1.0",
    "reconnecting-websocket": "^4.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "typescript": "^5.7.0"
  }
}
