# Extension: Sessions & Shortcuts (shim)
#
# This extension now embeds the framework_shells dashboard hosted by the
# framework_shells module (see `/fws/`).

from fastapi import APIRouter

router = APIRouter()

