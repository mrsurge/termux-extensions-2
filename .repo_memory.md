# TE2 Repository Memory

Condensed codebase orientation. Source is authoritative; this file is not a task
tracker, release log, or personal-preference store.
`CODE_TE2.md` below means `docs/apps/code_te2/CODE_TE2.md`; references include
section names and current line numbers. Refresh line references when moving the
manual's sections. Deep contracts and historical provenance belong there.

## Repository Safety And Source Ownership

- **IMPORTANT — launcher source names are misleading.**
  `desktop_client/android_shell/` is Electron-only launcher/settings source;
  its `android_shell` name is historical and does not make it shared with
  Android. `app/android_shell/` is the canonical GeckoView/Cefrium launcher
  source. `android/app/src/main/assets/editor_static/android-shell/` is bundled
  APK output. Trace the materialization path before editing or validating and
  never infer source ownership from the directory name.
- **IMPORTANT — native clients own their frontend assets.** Electron, GeckoView
  and Cefrium do not load Code TE2 UI bundles from the framework server. Their
  loopback/relay URL provides a stable origin and backend/proxy access; it is not
  frontend asset authority. After a frontend build, explicitly run that client's
  OTA/asset-update method (or bundle the new assets into its package/APK) before
  live validation. Page reloads only reload the client copy already installed and
  cannot pick up a newly built server-worktree bundle. Verify the client asset
  version/update result before interpreting any native-client frontend test.
- Follow `AGENTS.md` for approval, ownership and validation. Preserve unrelated
  edits, drafts and untracked files. Android changes need explicit scope.
- Never restart the shared TE2 runtime without approval. The known Ferrous
  cross-manager defect means `fws down` / `fws terminate` may kill the framework;
  use framework HTTP/Socket lifecycle endpoints for approved worker refreshes.
- Use `$TMPDIR` or a named workspace scratch directory, not hard-coded `/tmp`.
  Search source first; `.rgignore` excludes generated/vendor/worktree noise.
- Nested repositories are independent status/commit scopes. Root pytest excludes
  `worktrees/` to avoid collisions with their own top-level `tests` packages.
- Generated bundles, vendor publication and Android asset copies are outputs,
  not editable source. Validate nested projects from their repository roots.
- Keep this file concise and synchronized with technical documentation; remove
  superseded claims rather than accumulating progress reports or to-do lists.

## Framework Runtime

- Rust under `framework/rust/` is framework authority. `te2` / `te2-rust` enter
  through `app/cli/run_rust_framework.py` and the framework bootstrap, which
  selects the fingerprinted binary and starts the Python console/MCP bridge.
- Python remains for bootstrap/CLI, manifests/scaffolding, console/MCP, pipe
  support and app workers. Do not recreate the retired Python framework,
  supervisor, generic-extension backends, lifecycle or duplicate Git provider.
- Rust owns filesystem/Git/search, settings/state/bookmarks, app lifecycle,
  proxying and shell orchestration. Routine requests do not rediscover FWS
  metadata or scan `/proc`; explicit reconciliation owns discovery.
- Multi-threaded Tokio and bounded per-worker writer queues separate dispatch
  from blocking pipe writes. Queue saturation is explicit, not an event-loop wait.
- `app.libs.jobs` is core-only; no global `/api/jobs` or FastAPI/SSE router.
  Runtime, queue and retained-Python details: `CODE_TE2.md:4008` (§47).

- Runtime diagnostics startup opt-in uses `--runtime-debug` / `TE2_RUNTIME_DEBUG`;
  `--no-runtime-debug` overrides inheritance. Rust launch overrides manifest values.
  Status/eval use bounded live-loop dispatch; profiling remains separate.
  Shared writes serialize; duplicate replies cannot block
  stdin, and ordinary app dispatch stays unchanged (CODE_TE2.md §47).
- Rust debug status routes use exact app/shell/bridge-instance identity, one pending
  call and existing pipe I/O; timeout/disconnect cleanup never retries execution.
  CLI `te2 framework` and MCP share credential-protected Rust HTTP routes; MCP
  requires an explicit credential. Trusted eval is not sandboxed (CODE_TE2.md §47).
- WBA `wba-status/wba-eval` CLI and MCP reuse that authenticated Python route then
  its MessagePack pipe; exact child shell/process identity, no browser/HTTP eval.
  Live JS probes and bounded completion tracing: CODE_TE2.md, WBA Live Runtime Evaluation.
- Runtime-debug traces early Python imports/listener, Rust spawn/readiness and
  updated app-shell gate/init; clocks differ and init is not browser paint (§47).
- Lightweight service packages avoid eager re-exports. Run-profile projections
  reuse off-loop config reads per broadcast; no persistent cache (CODE_TE2.md §50).

- Runtime-debug shared proxy timing distinguishes WBA route arrival from upstream
  handshakes; bounded markers and closed-upstream regression test: CODE_TE2.md §50.
- Editor WBA sockets use `autoConnect: false` until the existing editor-lane
  adapter-ready snapshot/event; Monaco and document loading stay independent (§50).
- Networked app workers own paired async start/stop hooks outside ASGI lifespan;
  Code TE2 project initialization and eager intelligence use these hooks. Same
  process/loop; pipe-only mode unchanged. Cleanup contracts: CODE_TE2.md §50.
- App-worker serving readiness is released at Uvicorn's confirmed bound-listener
  boundary, not by a fixed lifespan delay. The asynchronous framework POST remains
  lifecycle-owned, so page release tracks the earliest real HTTP availability.
- Code TE2 opts into early intelligence bootstrap; only backend assembly moves
  off-loop. WBA attachment waits for code-server and app fact readiness. Contract:
  CODE_TE2.md §50, Early Intelligence Preparation And Backend Assembly.
- Framework/app-worker and WBA stdio pipes use concatenated MessagePack maps
  (32 MiB frame bound); upgrade both peers together. WBA stdout has typed records;
  Terminal log-framing remains pending. Details: CODE_TE2.md §50, Framework Pipe Codec.

## Framework Assets, Roots And Exposure

- Rust serves shell assets from `app/templates/` and `app/static/`; built-ins
  are in `app/apps/`, user app source in `~/.local/share/te2/apps`.
- Manifests own catalog/readiness contracts. App-shell startup consumes one
  app-scoped `/api/apps/events?app_id=...` SSE stream, not serialized boot GETs.
- `TE2_*_HOME` values are final roots; otherwise XDG plus `/te2` applies.
  `$TE2_DATA_HOME/app_state/<app_id>` is state; `apps/<app_id>` is source.
- Legacy migration is an explicit stopped-framework, dry-run-first command,
  never startup recovery. Published fonts are the four supported WOFF2 faces.
- Loopback is default. `--broadcast` resolves once to bind hosts and exposure
  policy; outer Axum middleware protects HTTP, SSE, raw WS, Socket.IO and proxies.
- IP/CIDR policy checks peers; interface policy checks local destinations.
  Public bind addresses never replace loopback `TE_FRAMEWORK_URL` for workers.
- Exact roots, migration, readiness and network rules:
  `CODE_TE2.md:4008` (§47), `CODE_TE2.md:4057` (Network Exposure).

## Code TE2 Layout And Identity

- Source is `app/apps/code_te2`; `main.py` assembles the app worker,
  `main.ts` boots the host, `monaco_editor/m_editor_app.ts` boots the editor.
- Host UI is `main_page/frontend/`; Explorer is `src/explorer/` plus
  `explorer/`; host actions are `host/`; UI/sidebar IPC is `ui_ipc/`;
  WBA is `workbench_protocol_proxy/`. Source map: `CODE_TE2.md:228` (§1).
- Public id `code_te2` is canonical. `file_editor_cm6` is only a bounded
  request/persisted-state migration alias, never another worker/catalog identity.
- Sidecar recents define bounded logical membership. Stable `clientInstanceId`
  owns one reconnectable foreground with primary/secondary role; `windowId`
  is presentation metadata. `last_file` is only a one-time migration seed.
- Shared close publishes a post-commit `DocumentClosed` fact: affected primaries
  move to MRU, secondaries clear, and exact-client editor state disposes/replaces.
- Frontend paths are projections, never cross-client authority. Foreground,
  shared revisions, drafts and persistence: `CODE_TE2.md:420` (§4).

## RPC And Event Boundaries

- Surfaces use their own lanes: editor `/rpc/editor`, Explorer `/rpc/explorer`,
  host `/ui_ipc`, sidebar `/sidebar_ipc`, drawer `/terminal`.
- Python namespaces share `/api/app/code_te2/socket.io`; direct editor/WBA
  uses `/api/app/code_te2/services/wba/socket.io`. Do not proxy intelligence
  through Python. Console uses `/te2_console_ws/socket.io`, `/te2_console`.
- Editor/Explorer/host/Sidebar app-worker connections currently try Engine.IO
  polling before WebSocket upgrade; WBA and terminal stay WebSocket-only. This
  experiment has no proven latency gain until live client A/B measurement.
- Editor, Explorer, host and WBA require strict `msgpack-v1`; no JSON fallback.
  Sidebar retains its established codec. Old full socket paths are aliases only.
- Requests emit only when connected; notifications are volatile. Disconnect
  rejects pending requests, clears buffered traffic, and reconnect reconstructs
  state. Do not enable Socket.IO recovery or queue stale editing traffic.
- Cross-surface intent goes frontend -> own backend -> target backend hook ->
  target notification. No private frontend-to-frontend lane shortcuts.
- Codec metadata is default-off via `CODE_TE2_RPC_CODEC_METRICS`. Python
  transport ingress validates `object` mappings rather than propagating Any.
- Transport/codec/ownership contracts: `CODE_TE2.md:188` (§0.6),
  `CODE_TE2.md:538` (§6), `CODE_TE2.md:625` (§6.5).

## Sidebar And Persistence

- Code TE2 data uses canonical data/config/runtime/cache partitions, not legacy
  CM6 roots. Preferences are config; drafts/sidecars are data; sockets runtime.
- Sidebar ledger v2 owns shared membership, stable slot identity and lifecycle;
  host-local presentation owns order, foreground and embedded/hidden/detached.
- Browser presentation is bounded origin/project Local Storage; Electron uses
  validated atomic config IPC; Android uses app-private storage partitioned by
  stable client, upstream origin and project. Random relay ports are not keys.
- Empty initial lists and WBA reset/disconnect snapshots are non-authoritative.
  Only complete authoritative snapshots or explicit removal prune membership.
  Inactive projects remain parked; no polling or grace-window recovery.
- Reconciliation filters by the host's current project. Backend activation replay
  cannot reveal a locally hidden view; explicit local user intent is required.
- Mentions/file opens require exact client, stable host and live presentation
  identity. Re-created hosts re-register; no global-active/broadcast fallback.
- Browser/Android Sidebar dock menus have one keyed DOM owner. Launcher taps and
  icon long-presses open once per gesture; the release click is consumed.
  Close all and kill bulk-removes the selected app's slots in the shared ledger,
  then invokes the existing app quit action. See `CODE_TE2.md` §44-45.
- ALS-RS sidebar traffic resolves injected `TE_FRAMEWORK_URL`, then `TE_PORT`,
  then standalone 8089, not its own 12459 listener. Reconnect registers its peer.
- Ownership, storage/migration, focus and exact-client routing:
  `CODE_TE2.md:3861` (§45), `CODE_TE2.md:3023` (§38).

## Search And Explorer Projection

- Rust `service.search` owns file/content search. Python search sessions adapt
  bounded cache/state; the frontend renders progressive events on its own lane.
- Name search projects inside the existing tree, preserves tree DOM/open state,
  expands hit ancestors and shallow-hydrates direct directory matches in one
  bounded `fs.listDirectories` request. No per-hit frontend listing RPCs.
- Backend directory-selection facts update all project clients before local
  dismissal. Sticky search controls retain their bound DOM/IME input.
  Sticky subtree limits use source directory border-box geometry, not estimates.
- Content search retains at most 700 logical hits. Multiple same-line matches
  are separate hits. Supersede, overlay close and project switch cancel sessions.
- Replace targets retained hits, not rendered rows; Show All/Select All expose
  them. Hidden-hit confirmation is separate from draft-discard consent.
- Mobile UA uses long-press/tap selection without checkbox DOM; file headers
  follow group-select/deselect semantics. Desktop uses checkboxes.
- By Contents file groups use one faint card outline around each single-line
  file header and its hits. Header controls never wrap; the path truncates while
  hit snippets retain their independent wrapping.
- By Changes emits its first verified diff before later candidate validation,
  then uses bounded ordered workers and independent Git handles for remaining
  previews. One emitter preserves tracked-first pagination/continuations. Python
  retains up to 700 objects; exact path facts avoid re-enumeration. Both search
  views expand early; By Changes reveals 40 more rows and retains them
  (`CODE_TE2.md:881`).
- Limits, edit identities, cancellation, progressive search, sticky projection:
  `CODE_TE2.md:726` (§6.6). Git comparisons: `CODE_TE2.md:1460` (§8).
- By Changes headers start collapsed with preview +/- counts, tail-clipped paths
  and filename-aware icons. Streaming retains local expansion; header toggles,
  row navigation and Restore remain independent (`CODE_TE2.md:881`). Expand All
  opens the current page's files/hunks/blinds; active literal filters reuse
  By Contents hit styling without replacing syntax/diff token spans.

- By Changes compact headers expose per-file Stage (+), never hunk staging, and
  Stage and commit all with an empty index, otherwise Commit selected. Both are
  HEAD-only; Restore keeps confirmation and uses an explicit historical label.
  The shared commit prompt rejects project/comparison changes before dispatch.
  SVG fe-btn remote actions reuse Git lanes; Fetch only updates remote refs,
  unlike Pull. History separates Fetch from its local Refresh control.

- By Changes streams tracked results first and defers untracked entries to the
  end of the same stable pagination order. Suppressed text bodies retain Git line
  statistics, including oversized tracked files; editor admission stays separate.
  Header totals cover retained tracked files with untracked additions separate;
  partial projections are labeled. Header-only MRU follows clicks/latest live delta.

- Explorer filename search has both a source-tree input and a separately created sticky-scope input. Mobile input attributes such as `autocapitalize=off` must be set on both; the sticky input can be the focused Android control even when the source input is correctly configured.

## Comparison And Guarded Edits

- One Explorer comparison ref drives tree/By Changes metadata, inline commit
  baselines and the far-left status drop-up. HEAD follows current Git facts;
  explicit history stays pinned. Disk mode disables autosave and skips Git reads.
- `GitDiffBaseChanged` owns selection invalidation. Independent latest-only
  projectors schedule reads without blocking the fact bus; matching selection
  revisions deduplicate snapshots without dropping worktree refreshes.
- Historical tree styles compare selected tree against disk; real HEAD/index
  status stays separate for warnings/actions. Actual-state warning icons appear
  only in historical mode and never become filename text.
- Historical stage/commit/reset are rejected by the backend. Restore writes into
  the current worktree, confirms drafts, offers path-scoped unstage and preserves
  the index. Never move HEAD or create a detached worktree for this view.
- Hunk Restore and Find/Replace operate on disk, regardless of autosave.
  Python owns expiring exact-client/project/revision consent and draft safety;
  Rust owns bounded SHA-256/exact-range guarded writes, not draft materialization.
- Reverse hunks come from canonical UTF-8 buffers/libgit2, not display rows.
  Failed/no-op writes retain drafts; newer drafts survive successful operations.
  Accepted mutations survive disconnect; uncertain writes are never retried.
- Guarded apply serializes same-root writes but cannot exclude arbitrary external
  writers between check and rename. Limits and index/draft semantics:
  `CODE_TE2.md:751` (Guarded Text-Edit Foundation), `CODE_TE2.md:828`
  (Search Replacement), `CODE_TE2.md:1460` (comparison/Restore).

## Source Control History

- Explorer History is summary-only: commit/ref graph, filenames and +/- counts.
  File clicks open immutable read-only historical diffs in the second editor.
  It is not WBA-backed and does not place document text in the graph.
- `src/explorer/history/vscode_scm/` contains literal attributed upstream SCM
  copies and pinned base-tree inputs, reproduced through explicit patches.
  Use its UPSTREAM.md, not a lookalike implementation or fake workbench services.
- Rust owns retained metadata revwalks, immutable first-parent file/blob reads,
  and ref/config/reflog watches. Python owns connection-local generations and
  bounded lazy statistics. Strict typed DTOs preserve captured ref/blob identity.
- Native sessions are exact pipe-owner/root/generation scoped. Lease expiry
  renews behind the same logical generation only when Rust snapshot identity and
  the replayed commit frontier match; otherwise the visible fallback refreshes.
  A project-local in-memory ledger restores expanded commit OIDs/page frontier
  across that real refresh and dies with the History surface. No polling,
  keepalive, persistent view state or historical-file intent replay is allowed.
- Metadata uses pure topological traversal with captured tips seeded newest-first;
  OID ordering or TIME traversal distorts branch continuity. Pinned current,
  upstream and base roles feed unchanged upstream lane colors.
- File statistics are independently bounded from preview admission. Partial totals
  remain partial, not falsely complete; binary/oversized preview states are explicit.
  File rows remain left-aligned and preserve full-bright basenames while a dimmed
  directory prefix clips from the repository/root side under width pressure.
- Frontend generations preserve row DOM, exact file indices, local selection,
  lazy expansion and single-flight early scroll prefetch. Fixed commit headers
  have the same compact shape on desktop and mobile. Mobile expanded cards and
  desktop hover panels share the full detail presentation and statistics without
  extra Git reads; localized timestamps live in their author byline, not the
  fixed header. Their short hash copies the full commit ID without activating
  the owning history row.
- Protocols, bounds, watches, upstream provenance and role resolution:
  `CODE_TE2.md:983` (§6.7).

## Historical Secondary Editor

- Backend secondary content state is distinct from working foreground and native
  presentation. File-open tickets are short-lived, bounded and single-use;
  authenticated secondary redemption validates retained project/session identity.
- Working/history kind changes reload only the secondary realm after command
  acknowledgement. Historical revisions abort/replace in place; duplicates reuse.
  Existing atomic persistence needs no additional save/flush, and membership stays.
- Historical boot loads basic/Monarch syntax only and allows the diff worker,
  not language services. Unique `te2-history:` models never enter WBA.
- Read-only controls reject Save/discard/mutation; navigation, selection, Find
  and Copy target the last focused diff side. No working-model fallback.
- Snapshot/live preferences project font/theme/line numbers/wrap through a safe
  allowlist. Theme requests are revision/abort-fenced; stale loads cannot publish.
- Theme picker metadata uses host RPC (`ui.host.themes.list`); Python projects
  only the preference-selected theme JSON via `editor.theme.selected` before
  working document models mount, with no catalog-wide frontend theme loading.
  Historical views fetch just their selected theme through the host lane.
  Monaco/TextMate styling is shared; see CODE_TE2.md §26.
- Editor HTTP assets are isolated in `monaco_editor/editor_asset_routes.py`;
  editor state has no web-framework imports. Backend grammar RPC remains separate
  from HTTP `onig.wasm`; see CODE_TE2.md, Generic WBA language path.
- Main HTTP retains only health/resources; legacy read/state/diff/review and raw
  read/edit-tracker/debug sockets are removed, not their shared services.
  Host file-read socket wiring is removed too; save replies and revision-checked
  editor cache events own host state. Load the rebuilt frontend to stop old retries.
  See CODE_TE2.md, Git And Project Transport Ownership, for the transport boundary.
- Code TE2 exports `TE2_ASGI_APP`: `http_app.py` composes Starlette resources and
  the existing Socket.IO gateway under Uvicorn, without FastAPI/Pydantic imports.
  Lifecycle remains worker-owned; other apps retain lazy FastAPI assembly.
  Details: CODE_TE2.md, Worker Transport Exports.
- Mobile helpers attach to both diff controls and dispose with them. Maintained
  touch fork `historicalReadOnly` allows Copy, Select Word/All, Find and Close,
  ignoring custom editing islands while leaving working tools unchanged.
- Exact lifecycle, handoff, syntax boot and preference/touch integration:
  `CODE_TE2.md:1296` (Secondary History Content State Foundation).

## Documents, Diagnostics And WBA

- WBA control uses the shared intelligence primer, shell-manager pipe and direct
  `/wba` socket, not legacy `/workbench_adapter/*` HTTP. See CODE_TE2.md,
  Generic WBA language path. Resource HTTP uses the native ASGI assembly.

- Sidecar membership defines logical open documents, not browser model residency.
  WBA retains one role-neutral extension-host document per canonical path and
  one synthetic editor facade per stable client.
- Reconcile protects retained facades plus in-flight opens. Releasing one facade
  cannot release another client's shared document; matching opens are idempotent.
- Admission precedes sidecar mutation: source files including .sh are accepted;
  known binary/media/object/font formats, extensionless executables and disk/draft
  content above 375 KiB are rejected.
- Python materializes only missing/stale background documents. Visible open
  completion is `editor.openComplete.publish`, never merely a WBA acknowledgement.
- Editor intelligence/diagnostics remain direct WBA-to-Monaco. Python diagnostics
  serve Explorer/Problems only; never nudge editor diagnostics from Python.
- Semantic prewarming is bounded retained-document caching with real full
  providers, exact identity and visible-open priority; never manufacture ranges.
- Documents/reconnect: `CODE_TE2.md:3283` (§39); semantic tokens:
  `CODE_TE2.md:2032` (§23); diagnostics: `CODE_TE2.md:2433` (§33).
- Code Inspector's Symbols mode reuses WBA documentSymbols, preserving hierarchy
  and selectionRange. Snapshots cap at 2,000 entries/32 levels; symbol rows jump
  without force-refreshing models. Touch action and protocol: CODE_TE2.md §40.

- Host file-open must forward symbol range, no-focus cursor placement and scroll
  intent to the shared editor-open validator; dropping these in the host payload
  silently loses Inspector target accents. Boundary tests cover the actual host
  handler plus editor notification (CODE_TE2.md §40).

- Code Inspector clear-highlight intent uses its host command lane and the retained
  request ID. The editor suppresses result-decoration replay for that inspection
  and clears the separate symbol-target decoration; a new inspection reenables
  highlights. Projection/results and search decorations remain (CODE_TE2.md §40).

- Runtime-debug cold-boot tracing is backend-gated by the selected-theme RPC. Backend theme source/hash uses stderr; frontend theme/TextMate/model/semantic milestones are bounded and text-free. Full semantic replies report the first invalid offset without changing tokens; delta contents are not inspected (CODE_TE2.md §26).

## Code Server And Extensions

- `webWorkersEnabled` selects mutually exclusive browser-worker and managed
  Code Server modes. Worker mode does not probe WBA; neither mode silently falls
  back. Direct provider dispatch matches all selectors, not language-specific hacks.
- Code Server is pinned/private/confirmation-gated. System/PATH/NVM installs and
  executable overrides are not supported runtime authority.
- Config `intelligence.json` owns mode/install state; PreferencesStore projects it.
  Launch failures/worker mode clear availability. Imported `pinned-rpc-ids.ts`
  ships with WBA; no NID/version probes
  (CODE_TE2.md §30 and Preference-owned Code Server installation state).
- User settings edit the
  private User settings file; Workspace settings edit active .vscode/settings.json.
- Open VSX installs verify artifact identity, allowlisted download origin and
  published SHA-256 before managed local-VSIX install. No server-gallery fallback.
- UI VSIX remains bounded: activity webviews/panels, opaque sandboxing, theme,
  recovery, commands, menus and storage use existing contracts. The legacy
  file-watcher extension API bridge is removed; do not restore it.
- Modes: `CODE_TE2.md:1679` (§11). Installer: `CODE_TE2.md:3448` (§41).
  Extension settings: `CODE_TE2.md:1990` (§22). UI VSIX: `CODE_TE2.md:3706` (§44).
- WBA process boot overlaps code-server readiness after spawn/config; connection
  waits under single-flight ownership. Monaco mounts independently (CODE_TE2.md §50).
- Managed shells use optional private NODE_COMPILE_CACHE service directories;
  cache failure is nonfatal and Bun support is not assumed (CODE_TE2.md §11).
- WBA I/O shares bounded stdout backpressure and geometric pipe buffering across
  Node/Bun; only file reads specialize for Bun. Health/startup reports the selected
  runtime; Socket.IO stays Node-compatible. Worker background discovery prefers
  Bun, but WBA launch never waits and defaults to Node while discovery is pending
  (CODE_TE2.md §50, WBA Runtime I/O).

- WBA sidebar activation runs outside connection readiness; reset cancels provider
  waits and fences stale publication. Bounded debug milestones distinguish first
  document/language/provider/diagnostic events (CODE_TE2.md §50).
- Direct-WBA replay is socket-scoped, while initial intelligence application is
  model-scoped. If WBA connects before Monaco's first model, retain the deferred
  pass and complete open/provider/diagnostic/semantic synchronization on model
  attachment; late matching semantic providers invalidate the active model.
- WBA pushes semantic-provider registration/change events and diagnostics;
  attached Monaco models pull semantic-token DTOs. Full/range semantic pulls use
  a 30-second ext-host budget within a 36-second outer RPC envelope and reject
  late canceled results, avoiding the former cold-basedpyright 12-second cutoff.

- TextMate catalog/body projection is backend-owned and available through typed
  editor RPC before WBA. The persisted extension registry stores grammar descriptors,
  path-language associations and one revision; bodies are lazy and bounded. Theme,
  catalog/factory and the active grammar complete before first model creation, after
  that exact client's authenticated editor RPC connection resolves. Socket construction
  is not readiness and one warm client cannot satisfy another client's barrier. Later
  model replacements use the same barrier. WBA configuration/intelligence
  attach afterward. Registry changes dispose/reinstall active providers without
  focus; there is no WBA/HTTP fallback or full-corpus payload (CODE_TE2.md §26).
- Completion replies carry original compact DTOs once; frontend provider-specific
  conversion, resolve/dispose and session fencing follow VS Code. Provider waits
  do not hold the document gate (CODE_TE2.md, Compact Completion Projection).

- Cold live SSOT can displace the boot snapshot before Monaco reads its path.
  Boot loads the projected grammar catalog without a path; SSOT awaits its
  active grammar and rechecks snapshot/revision identity before mounting.
  A pre-fix Rust model briefly mounted as `plaintext` despite the correct
  `github-dark` theme and Rust catalog entry (CODE_TE2.md §26).

## Editor UI And Mobile Input

- Host menus, tabs and Monaco stacking follow one policy: Monaco host z-index
  auto, widgets at 300; opened host surfaces isolate the editor below them.
- Tabs project open/Git/draft/diagnostic facts. Draft asterisk is structural and
  discard is confirmation-gated; decorations must not reload sidecars off-loop.
- Fresh preferences use draft mode, diff overlays off and vendored github-dark-default;
  explicit persisted choices override seeds. Product preferences are not agent policy.
- Inline-diff boot starts plain, then baseline application promotes to diff.
  Enabled flags do not prove initialization. Wrap native timers to retain receiver.
- Android Monaco keeps the detached guarded whole-line textarea, coalesced input
  and inputType-aware newline handling. Native EditContext is disabled on Android;
  do not reintroduce raw-key newline suppression or composition-based gating.
- Primary host owns the shared key dock; exact-source/origin secondary presentation
  messages select its focused editor. Terminal focus is a temporary override.
  Floating rails belong to the focused realm, not the primary unconditionally.
- Hold-repeat snapshots target/modifiers once and cancels on release/lifecycle.
  Fullscreen drawer layout preserves status/key rows; Collapse retains secondary
  rendering while Close clears/disposes. Empty/non-terminal drawer opens create no PTY.
- Monaco touch coordinates already include the gutter. Resync layout/content-size
  changes; never subtract stale initialization margins. Read-only mode is fork-owned.
- Touch source/publication: `CODE_TE2.md:2401` (§32); input/key/drawer behavior:
  `CODE_TE2.md:2481` (§35); stacking/comparison: `CODE_TE2.md:1460` (§8).

- Shared file icons resolve through `app/static/vendor/seti-icons/seti-icons.js`;
  its `.mjs` alias uses the vendored `.js` SVG/color without altering mapping data.
- Second-row Shift is one-shot and distinct from sticky Sel; guarded Ctrl-byte
  replay retains its Shift/Alt snapshot. Ctrl+Shift+P/O invoke existing Monaco
  quick picks. Extra keys preserve palette focus; committed-text uppercase never
  rewrites active composition or guesses keyboard-layout punctuation (§35).
- Android spacebar-slide uses selection-only events, not key sequences. The fork
  maps unchanged guarded line offsets to model selection without reseeding;
  pending text transactions retain ownership in both renderers (CODE_TE2.md §35).

- Monaco text gestures target `.lines-content`; touch-fork handles live outside
  it under `.overflow-guard`. Trace handle hit testing separately from native
  gestures. The hover-widget hold timer only drags an existing hover; automatic
  hover consumes editor mouse movement. Source ownership: CODE_TE2.md §32.

- Editor-only source gesture arbitration selects held words without IME focus,
  tolerates tap jitter and irreversibly classifies scrolling. Caret handles defer
  hit testing through the double-tap window; teardrops retain drag ownership.
  Ctrl+vertical arrows reuse native blank-line movement, including selection and
  historical views. Terminal/palette routing stays separate (CODE_TE2.md §32).

- Contents search and Code Inspector References/Implementations/Call Hierarchy share a subtle result decoration with minimap/overview markers. Document Symbol targets are outline-only with the same navigation markers. Search-overlay close publishes an authoritative clear even after frontend state skew.
- Editor cursor shape is a validated PreferencesStore option: `line`, `block`, or `block-outline`; invalid persisted values migrate to `line` and Editor Settings projects changes through the normal preference lane.

## Terminals

- Standalone Terminal uses Node/node-pty/headless-xterm; Code TE2's drawer uses
  Python/Pyte. Preserve distinct ownership while sharing input/touch helpers.
- Standalone PTY is raw MessagePack WS; lifecycle/control is websocket-only
  Socket.IO. Node owns checkpoints/sequence/scrollback; shell lists come from
  one FWS snapshot plus events. Active selection is client-local.
- Two-row soft keys compose modifiers, repeat arrows and guard minibar opening.
  Keys/Show exited are local, temporary controls. Dash follows guarded input.
- Drawer control/history uses its existing /terminal Socket.IO lane, not HTTP.
  Pyte parses canonical raw-log bytes off-loop; FWS chunks are wakeups, not text.
- Checkpoints contain ANSI screen state, decoder prefix and exact offset.
  Live binary deltas are offset/generation-fenced; no raw-tail replay, textual
  overlap guesses, decoded fallback, or polling.
- Resize marks Pyte projection stale; next checkpoint reparses at requested
  dimensions. Do not resize retained screens or replay on routine viewport changes.
- Shared Android xterm cumulative input is printable-text authority; keypress
  fallback is bypassed, modified/non-text keydown remains authoritative, and
  guard repair plus the source input hook handle stuck Gboard/229 behavior.
- Xterm source owns the transparent scroll surface and irreversible gesture
  classification; the mobile adapter owns anchored selection, handles and menu.
  No synthetic mouse lifecycle for handle drag; completed taps alone replay it.
- Standalone controls: `CODE_TE2.md:4286` (§48); drawer: `CODE_TE2.md:3940`
  (§46); shared input/touch source, publication and offset rules: §35 at line 2481.

- The shared status-bar Panel control toggles the bottom drawer in its last available tab; tab choice is page-local and drawer visibility uses a local event. Restoring a non-terminal tab must not create a PTY.
- Code TE2's drawer preloads its declared JetBrains Mono Nerd face through the vendored xterm web-font addon before opening xterm, with a nonblocking browser-font fallback.

## Native Clients

- GeckoView is primary Android; Cefrium is isolated, not a system-WebView fallback.
  Android settings and stable identities are application-private per APK.
- Cefrium 0.9.0 uses a standalone `android/cefrium` Gradle build (JDK 25/SDK 37),
  with explicit shared variant Kotlin sources; Gecko stays separate. CODE_TE2.md §42.
- Cefrium 0.9.0 supplies its Window Extensions R8 consumer rule, but Gradle drops
  the POM's Maven `provided` interface dependency. Retain TE2's extracted
  compile-only `window-extensions-core-1.0.0.jar`; do not restore the duplicate
  local keep rule.
- Cefrium's main browser registers its controllable download handler before page
  navigation. Requests serialize Android `CreateDocument` pickers, stage bytes
  under app-private cache, then copy completed content to the selected
  `content://` document off the UI thread. Cancellation, interruption and
  teardown remove owned partial state; no broad storage permission is used.
- The native runtime retains the configured `frameworkBaseUrl` and retargets its
  app-owned localhost relay. Browser API, Socket.IO and WebSocket traffic continue
  through that relay to preserve the existing browser-security model.
- Android assets come only from OTA/APK materialization; declared missing files
  fail closed. Block TE2 /sw.js and use scoped cleanup, not a blanket SW ban.
- PersistentNetworkService owns relay/routes/UI IPC/console across activity
  recreation. Retargeting runs through configure; activities do not duplicate it.
- Route projection and inspector targets are separate. Cold restore needs a fresh
  authoritative route snapshot, not persisted routes. No native presentation polling.
- Gecko WebExtension and Cefrium native-query bridges carry stable client/secondary
  identity and presentation state. Reset rotates primary/secondary atomically.
- Cefrium's native multi-touch/double-tap zoom controls are not complete zoom
  suppression. Keep the renderer-scoped 16px Monaco IME/Find input floor.
- IME context switching is opt-in. Permission/power refresh is foreground-event
  driven, and both activities resynchronize the persistent service on resume.
- Electron owns its trusted shell origin, framework WebContentsView, loopback relay,
  validated IPC and stable distinct editor identities; launch uses --no-sandbox.
- Electron bootstrap reconnects its existing native UI IPC client when the worker
  becomes ready. An early namespace refusal must not strand route readiness.
- Electron manual Connect and bookmark selection use the same immediate
  main-process connection transaction.
  Electron startup checkboxes and preferred-app selection persist immediately
  through one serialized settings queue; host/port changes retain their explicit
  connection transaction.
  Local-process ownership and immutable launch config are distinct from edits to
  next-launch settings. Desktop settings independently control local-framework
  startup and preferred-app opening; fresh managed installs enable the former.
  Startup pipelines local adoption/spawn and the preferred-app open request with
  launcher boot; app-view navigation is renderer-independent, while shell readiness
  only replays current navigation and deferred errors into the native header.
  When both startup options select a preferred app, the launcher iframe is lazy
  and loads only on explicit home navigation or startup failure.
  Health/collision and post-spawn readiness checks remain authoritative.
  Electron exit awaits shutdown only for its owned child, never an external local
  or remote target.
- The distinct Electron and Android launcher implementations both use
  right-click or touch long-press on app cards for one SVG `X Close` action;
  no always-visible destructive ellipsis remains.
- Android assets: `CODE_TE2.md:2632` (§36); Electron/secondary: §38 at line 3023;
  Cefrium: §42 at line 3503; Gecko inspector: §43 at line 3619.

- Android Settings exposes a no-poll, same-origin asset status/update control.
  The shared gateway only schedules the existing renderer-native OTA path;
  GeckoView/Cefrium own single-flight download, validation, atomic install,
  cache clearing, feedback and reload.

## Console And Shared UI

- TE2 console is frontend runtime observability, not shell stdio. Discover exact
  live worker IDs before eval; labels can represent multiple windows/clients.
- `te2 console list-workers/eval` require a live framework; tail/search read
  retained transcript. Bounded replay and volatile telemetry avoid socket backlogs.
- Framework-Shell logs are separate; inspect structured metadata before raw logs.
  Raw historical lines have no invented per-line timestamps; stdin is opt-in.
- Async teUI dialogs replace browser prompts; Electron uses same-origin portal
  child windows. Popup listboxes belong to the dialog layer, not clipped bodies.
- Dependency-free component-runtime owns synchronous JSX/refs/disposal, not state
  authority or a VDOM. Modal-kit re-exports its JSX runtime; keep CM6 JSON fields.
- Console: `CODE_TE2.md:2159` (§25); host/UI source: §1 at line 228;
  FWS execution/transport distinction: §0.5 at line 149.

## Build And Publication

- Both Android debug variants disable minification for readable stacks and native
  member inspection; release/staging retain their separate build policies.
- Debug native reflection uses one main-thread dispatcher via console JSON or
  DUMP-protected ADB broadcasts; bounded weak handles/tracing, no Kotlin compiler.
  Release/staging compile inert seams. Commands/limits: CODE_TE2.md §50,
  Android Debug Runtime Inspection.

- Frontend validation/publication: run npm run typecheck and node build.mjs in
  app/apps/code_te2. Host output is static/dist/host.js; do not revive editor.js.
- Native contract handoffs synchronize release-facing versions and build frontend
  before approved Android asset bundling/APK builds. Never seed stale assets.
- Keep Cargo caches. Framework builds are optimized unless --debug; publication
  validates a selected fingerprinted binary under the shared build lock.
- Installed releases use verified native artifacts with explicit provenance and
  --build-info contracts; binary installs cannot silently fall back to Cargo.
- Linux uses private pinned Node/npm/headers from nodejs-wheel; Termux uses its
  declared apt runtime. Node-pty needs build-essential on supported Linux installs.
- Managed Linux `te2-desktop` dispatches through the stable installer-owned
  `te2 desktop launch` command. Once desktop integration has a receipt, every
  Linux upgrade reconciles it, and launch verifies the current source fingerprint
  before starting Electron; never delete valid Sidebar state to repair skew.
- The current first-party release baseline is TE2 0.2.350, Framework-Shells
  0.0.64, Agent Log Server 0.2.131 and Ferrous Framework tag 0.2.14 at
  `4cdf5db`; Python requirements use published wheel pins and Cargo uses the
  Ferrous release tag.
- Dependency locks, native ABI tags, FastMCP compatibility, clean wheel staging,
  Termux/Linux installation and current-plus-one rollback are documented in
  `CODE_TE2.md:4073` (§47 Build And Installation Contracts).
- Historical checksums/tags/acceptance belong in `CODE_TE2.md:4330` (§49), not
  always-loaded memory. Treat them as publication-time evidence, not current status.

- Monaco bootstrap is statically embedded into Code TE2's `host.js`, not loaded as
  a separate runtime script. Publish changed Monaco ESM, rebuild with
  `node scripts/build_monaco_bootstrap_bundle.mjs`, then run Code TE2's
  `node build.mjs` in that order. Rebuilding only bootstrap leaves host behavior
  unchanged even after a successful native asset update (CODE_TE2.md §32).
