# Repo Memory Snapshot - Pruned Draft

Draft replacement for `.repo_memory.md`, generated from a read-only pass on
2026-06-02. This draft keeps current durable repo facts and intentionally drops
slice-by-slice version history, workflow policy notes, duplicated sections, and
facts superseded by later implementation.

## Orientation And Authority

- Fact: Repo workflow policy belongs in both `AGENTS.md` and `.repo_memory.md`. Unless the user explicitly says `approved` or directly orders a workflow bypass, agents must do read-only investigation first, summarize findings/proposed scope, use `ask_user` for approval, edit only the approved scope, and stop for new approval when scope changes.
- Fact: Prefer current source over older planning docs when they disagree. `docs/apps/code_cm6/CODE_TE2.md` and `docs/planning/FILE_EDITOR_CM6_REFACTOR_NORTH_STAR.md` remain useful first references, but some older sections still describe historical paths such as `src/host`, `vscode_api`, or pre-collapse Socket.IO topology.
- Fact: Current live `file_editor_cm6` version surfaces are synchronized at `0.2.256` across `pyproject.toml`, `app/apps/file_editor_cm6/manifest.json`, and `app/apps/file_editor_cm6/static/version.txt`.
- Fact: For served Code TE2 frontend changes, rebuild `app/apps/file_editor_cm6` and sync the three live version surfaces before handoff. Android bundled asset publication is a separate path and should not be bumped unless intentionally republishing bundled Android assets.
- Citations: `AGENTS.md`, `docs/apps/code_cm6/CODE_TE2.md`, `docs/planning/FILE_EDITOR_CM6_REFACTOR_NORTH_STAR.md`, `pyproject.toml`, `app/apps/file_editor_cm6/manifest.json`, `app/apps/file_editor_cm6/static/version.txt`

## Runtime Territory

- Fact: TE2 runtime-wide transports and services that must survive app-worker/editor-worker restarts belong under `app/` and mount from the framework, not from `file_editor_cm6`.
- Fact: App-scoped main-process services belong under `app/apps/<app_id>/services/` or manifest-declared service/proxy config. Worker-lifetime features belong in the app worker or worker-owned transports.
- Fact: The live TE2 console transport is framework-owned: namespace `/te2_console`, path `/te2_console_ws/socket.io`, backed by `app/te2_console_runtime.py` and mounted by `app/te2_runtime_mounts.py`. UI IPC does not relay `console:*` events.
- Fact: `te2_mcp` SSE and streamable HTTP are TE2 runtime-owned mounts at `/te2_mcp` and `/te2_mcp_http`; they are not `file_editor_cm6` worker services.
- Citations: `app/te2_console_runtime.py`, `app/te2_runtime_mounts.py`, `app/main.py`, `app/te2_mcp/server.py`, `app/apps/file_editor_cm6/ui_ipc/ui_ipc_ws.py`

## App Framework

- Fact: `/api/apps` is served from the in-memory apps registry snapshot. Manifest metadata changes, including `entrypoints.frontend_script` query bumps, need `/api/apps/reload` to refresh the registry without restarting the framework.
- Fact: App-shell quit actions should use `/api/apps/{app_id}/quit`, which delegates through app runtime shutdown and framework-shell app-group shutdown.
- Fact: App manifests can declare `sidebar_state`; the app catalog preserves that capability and app lifecycle stores semantic readiness separately from shell/TCP readiness.
- Fact: Semantic app readiness is posted to `POST /api/apps/{app_id}/readiness`. For stateful sidebar app windows, readiness payloads should include `host_id`, `token_id`, `url`, and when available `console_worker_id`.
- Fact: `app/apps/als_rs` is the tracked builtin ALS-RS proxy-shell wrapper. Its manifest identity is `id: "als-rs"`, while the builtin directory name is `als_rs`.
- Citations: `app/extensions/apps/main.py`, `app/extensions/apps/registry.py`, `app/extensions/apps/runtime.py`, `app/libs/app_lifecycle.py`, `app/templates/app_shell.html`, `app/apps/als_rs/manifest.json`

## Stateful Sidebar App Windows

- Fact: `/sidebar_ipc` owns the typed sidebar window ledger persisted in UI preference `sidebarWindowState`.
- Fact: Current window/app methods include `sidebar.launcher.catalog.get`, `sidebar.windows.list`, `sidebar.window.create`, `sidebar.window.openUrl`, `sidebar.window.activate`, `sidebar.window.close`, and `sidebar.window.readiness.update`. The server publishes `sidebar.windows.changed`, `sidebar.window.activated`, and `sidebar.window.readiness.changed`.
- Fact: `app/apps/file_explorer` is the reference POC for sidebar stateful app windows. It declares path-state support in `manifest.json`, uses `te2_host_id` and `te2_token_id`, publishes state URL updates, and posts framework readiness from `file_explorer.py`.
- Fact: The older memory claim that the catalog method is `sidebar.apps.catalog` is stale; use `sidebar.launcher.catalog.get`. The older helper-file citations `app/libs/sidebar_ipc_client.py` and `app/libs/app_readiness.py` are stale in this checkout.
- Citations: `app/apps/file_editor_cm6/ui_ipc/sidebar_rpc_contract.py`, `app/apps/file_editor_cm6/ui_ipc/sidebar_window_state.py`, `app/apps/file_editor_cm6/ui_ipc/sidebar_ws.py`, `app/apps/file_editor_cm6/preferences_store.py`, `app/apps/file_explorer/manifest.json`, `app/apps/file_explorer/main.js`, `app/apps/file_explorer/file_explorer.py`

## Code TE2 Source Ownership

- Fact: Current host entrypoint is `app/apps/file_editor_cm6/main.ts`, bundled by `build.mjs`. The old `main.js` wording in older memory should be read as historical unless source confirms it.
- Fact: Host/main-page chrome and orchestration source now lives under `app/apps/file_editor_cm6/main_page/frontend/`. The old `src/host` tree and legacy `static/js` source path are gone.
- Fact: Editor UI/runtime source belongs under `app/apps/file_editor_cm6/monaco_editor/`; the active entry is `m_editor_app.ts`, not `m_editor_app.js`.
- Fact: Explorer UI/runtime source belongs under `app/apps/file_editor_cm6/src/explorer/`. Shared diagnostics UI lives under `src/diagnostics/`; shared RPC constants/utilities live under `src/rpc`, `src/ui_ipc`, or `src/sidebar_ipc`.
- Fact: Sidebar shortcut source is now strict TypeScript under `main_page/frontend/sidebar-shortcuts/`. Historical `agent*` DOM ids and preference names may remain as compatibility names, but the old in-app agent harness was removed from the live source path.
- Citations: `app/apps/file_editor_cm6/main.ts`, `app/apps/file_editor_cm6/build.mjs`, `app/apps/file_editor_cm6/main_page/frontend/`, `app/apps/file_editor_cm6/monaco_editor/m_editor_app.ts`, `app/apps/file_editor_cm6/src/explorer/`, `app/apps/file_editor_cm6/main_page/frontend/sidebar-shortcuts/`

## Socket.IO And RPC Topology

- Fact: `app/apps/file_editor_cm6/socketio_gateway.py` creates one worker-owned Python `socketio.AsyncServer` for `/rpc/editor`, `/rpc/explorer`, `/ui_ipc`, `/sidebar_ipc`, and `/terminal`.
- Fact: The old per-surface Socket.IO modules are import-compatible shims over the shared server/app. They no longer create separate `AsyncServer` instances.
- Fact: `app/apps/file_editor_cm6/sio_service.json` is the framework-owned raw Socket.IO proxy config. It exposes the app-worker route and legacy aliases `/editor_ws/socket.io`, `/explorer_ws/socket.io`, `/ui_ipc_ws/socket.io`, and `/terminal_ws/socket.io`.
- Fact: WBA remains adapter-owned and behavior-separate. The framework proxy exposes `/wba_ws/socket.io`, but it is proxy-only and does not move WBA behavior into the worker or framework.
- Fact: Shared Python JSON-RPC envelope validation/conversion for the app Socket.IO edge lives in `socketio_jsonrpc.py` and uses `msgspec`.
- Citations: `app/apps/file_editor_cm6/socketio_gateway.py`, `app/apps/file_editor_cm6/socketio_jsonrpc.py`, `app/apps/file_editor_cm6/sio_service.json`, `app/apps/file_editor_cm6/main.py`, `app/extensions/apps/sio_service.py`, `app/apps/file_editor_cm6/manifest.json`

## UI IPC And Sidebar IPC

- Fact: `/ui_ipc` is host/runtime IPC. Editor-originated commands enter through the editor-owned RPC namespace and may be relayed to `/ui_ipc` only when the host is the consumer.
- Fact: `/sidebar_ipc` accepts typed JSON-RPC envelopes on the `rpc` and `rpc.notify` events. Legacy Socket.IO event names such as `sidebar:event`, `sidebar:mention`, `sidebar:cwd_set`, `sidebar:agent_open`, `sidebar:agent_edit`, and `sidebar:presence` are no longer live behavior.
- Fact: Sidebar mention fanout uses typed `sidebar.mention`; Explorer, host diagnostics, and editor mention hooks should publish typed notifications rather than importing a shared Socket.IO server or dual-emitting legacy events.
- Fact: Sidebar project hooks are `sidebar.project.lookup`, `sidebar.project.open`, and `sidebar.project.create`, with server notification `sidebar.project.opened`. `sidebar.cwd.get` is the current-project path endpoint.
- Citations: `app/apps/file_editor_cm6/ui_ipc/ui_ipc_ws.py`, `app/apps/file_editor_cm6/ui_ipc/sidebar_ws.py`, `app/apps/file_editor_cm6/ui_ipc/sidebar_rpc_contract.py`, `app/apps/file_editor_cm6/src/sidebar_ipc/rpc_contract.ts`, `docs/apps/code_cm6/SIDEBAR_IPC_RPC_SCHEMA.md`

## Explorer RPC

- Fact: `/rpc/explorer` no longer fabricates synthetic legacy inbound `{type,payload,id}` envelopes. `explorer/transport/rpc_socketio.py` parses JSON-RPC, resolves the dispatcher message type, and calls `ExplorerDispatcher.dispatch_message(...)` directly.
- Fact: `ExplorerDispatcher.dispatch_message(...)` is the parsed-command execution seam. Explorer feature families now live under `explorer/contracts/*`, `explorer/handlers/*`, and `explorer/services/*`, with `explorer_runtime.py` acting as composition/dispatch glue.
- Fact: Explorer-originated editor opens use typed Explorer RPC method `explorer.editor.open`; the renderer sends one backend open request with optional line/column and project root, and backend/editor logic owns same-file jump/no-op behavior.
- Citations: `app/apps/file_editor_cm6/explorer/transport/rpc_socketio.py`, `app/apps/file_editor_cm6/explorer/transport/rpc_contract.py`, `app/apps/file_editor_cm6/explorer_runtime.py`, `app/apps/file_editor_cm6/explorer/contracts/`, `app/apps/file_editor_cm6/explorer/handlers/`, `app/apps/file_editor_cm6/explorer/services/`

## Editor RPC And Open State

- Fact: `/rpc/editor` is the editor-owned typed RPC lane for editor state, open/save/mirror/cache notifications, edit commands, focus/blur, mentions, and agent-edit review methods.
- Fact: The visible editor-open completion boundary is the Monaco/open-transaction path, not adapter-side `vscode.openFile`. Treat `editor_open_complete` as valid only after the active Monaco model URI matches the target and explicit line/column navigation has been applied.
- Fact: The open-file authority is the active project's `ProjectSidecar.last_file`, surfaced through `open_state_backend.py`. Frontend `currentPath`, host/editor session state, Explorer active markers, WBA active document state, and history MRU are projections or compatibility data.
- Fact: Backend open paths write sidecar open state and publish sidecar-derived events across editor RPC, UI IPC, and Explorer projections. If a backend open path lacks sidecar writer/event-emitter wiring, it should fail rather than silently using an older authority.
- Fact: Project switch uses the shared `explorer.services.project_switch.switch_project_connection(...)` service across Explorer, sidebar IPC, and retained HTTP routes. The stale `/editor/set_active_project` route is removed.
- Fact: Project lifecycle completion events such as `ui.project.switched` / `editor.project.switched` must not directly overwrite the host toolbar filename. Toolbar filename updates flow through open-state / active-file events.
- Citations: `app/apps/file_editor_cm6/monaco_editor/editor_rpc_contract.py`, `app/apps/file_editor_cm6/monaco_editor/editor_rpc_contract.ts`, `app/apps/file_editor_cm6/monaco_editor/editor_rpc_dispatch.py`, `app/apps/file_editor_cm6/monaco_editor/editor_open_transaction_runner_main.ts`, `app/apps/file_editor_cm6/open_state_backend.py`, `app/apps/file_editor_cm6/project_sidecar.py`, `app/apps/file_editor_cm6/monaco_editor/editor_ws.py`, `app/apps/file_editor_cm6/explorer/services/project_switch.py`

## Workbench Adapter And Code-Server

- Fact: `file_editor_cm6` editor intelligence uses the direct WBA Socket.IO lane: namespace `/wba`, path `/wba_ws/socket.io`. WBA-owned methods/notifications should not fall back to `/rpc/editor` or legacy `editor_workbench_*` Python relays.
- Fact: Provider registration and raw diagnostics editor notifications arrive through WBA `te2.event` notifications; `diagnostics_bridge.py` keeps normalized explorer/problems diagnostics.
- Fact: The WBA control plane remains a live FWS pipe RPC path for backend adapter operations. `adapter_rpc()` writes JSON-RPC into the adapter process through framework-shell pipe capabilities and the stdout reader resolves pending responses.
- Fact: Current WBA code-server target is Unix domain socket based, using `~/.config/code-server/code-server.sock` and `TE2_CODE_SERVER_SOCKET`; stale adapters with an empty socket target should be treated as old TCP-target shells and respawned.
- Fact: The dead `vscode_rpc` side-channel and dead `vscode_api` runtime harness are removed from the live source path. Current docs should treat `vscode_api` / `vscode_rpc` sections as historical unless a current source path proves otherwise.
- Citations: `app/apps/file_editor_cm6/monaco_editor/editor_wba_rpc_transport.ts`, `app/apps/file_editor_cm6/monaco_editor/editor_wba_runtime_handlers.ts`, `app/apps/file_editor_cm6/workbench_adapter_shell_manager.py`, `app/apps/file_editor_cm6/code_server_shell_manager.py`, `app/apps/file_editor_cm6/shellspec/code_server.yaml`, `app/apps/file_editor_cm6/workbench_protocol_proxy/node_workbench_adapter/src/`, `docs/planning/FILE_EDITOR_CM6_VSCODE_API_REMOVAL_AUDIT.md`

## Language Intelligence

- Fact: WBA completion-provider push/cache is the authority for live Monaco completion bridge registration. Completion registration must not depend on the removed `vscode_api` path.
- Fact: Direct WBA calls wait at the shared WBA transport boundary for Socket.IO readiness. Document-backed WBA language-feature calls wait for matching active-model `open_file` ack before invoking providers.
- Fact: Semantic-token refresh is event-handle driven: WBA preserves provider `eventHandle` metadata and forwards semantic-token did-change events so the editor bridge can fire Monaco provider invalidation for the matching language.
- Fact: TextMate tokenization uses vendored `vscode-textmate` / `vscode-oniguruma` plus workbench-derived TextMate helpers. Grammar catalog/load goes through the WBA textmate RPC path, not `vscode_api`.
- Fact: Nid auto-discovery in `extension_registry.py` extracts numeric `rpcId` values from code-server's minified `extensionHostProcess.js`, caches them at `~/.config/code-server/te2_rpc_config.json`, and adapters should load named `_rpcIds` lookups rather than hardcoding nids.
- Fact: VS Code `vscode://schemas/*` settings schemas are renderer/internal schema-registry content, not `TextDocumentContentProvider` output. Keep this as external code-server reference evidence, not a current repo transport.
- Citations: `app/apps/file_editor_cm6/monaco_editor/editor_language_bridge_providers.ts`, `app/apps/file_editor_cm6/monaco_editor/editor_workbench_runtime.ts`, `app/apps/file_editor_cm6/monaco_editor/editor_wba_rpc_transport.ts`, `app/apps/file_editor_cm6/monaco_editor/editor_textmate_runtime.ts`, `app/apps/file_editor_cm6/workbench_protocol_proxy/node_workbench_adapter/src/extensions/`, `app/apps/file_editor_cm6/extension_registry.py`, `~/.config/code-server/te2_rpc_config.json`

## Diagnostics And Explorer Search

- Fact: The old `/lsp_ws` + `/api/lsp/*` + Pyright scan/workers/config path is removed. Explorer diagnostics now flow through typed RPC notification `explorer.diagnostics.detail`.
- Fact: Diagnostics correctness boundary: extension `$changeMany` is diagnostics publication and must be routed per owner/resource; editor keystroke document sync is `$acceptModelChanged`.
- Fact: Explorer `content` search uses the vendored VS Code-style search header under `src/explorer/search/content-query-widget.ts`. `explorer.search.run` carries content-search options such as regex, case sensitivity, whole words, include/exclude patterns, and ignore-file behavior.
- Fact: `static/dist/host.css` is part of the active vendored search-header styling path and must be loaded with the explorer stylesheet for the header to render correctly.
- Citations: `app/apps/file_editor_cm6/diagnostics_bridge.py`, `app/apps/file_editor_cm6/src/explorer/rpc/contract.ts`, `app/apps/file_editor_cm6/src/explorer/search/content-query-widget.ts`, `app/apps/file_editor_cm6/src/explorer/search/controller.ts`, `app/apps/file_editor_cm6/explorer/search.py`, `app/apps/file_editor_cm6/template.html`

## Drafts, Diffs, And Save Modes

- Fact: Editor save mode is canonized as Auto Save vs Draft Mode. Disk-vs-draft diff and commit diff are mutually exclusive diff modes; disk-vs-draft forces Draft Mode because it is invalid while autosave is on.
- Fact: The TE2 pinned Monaco diff model API is removed from the VS Code fork. `IDiffEditorModel` no longer exposes `originalBaseline`, `modifiedBaseline`, `te2FreezeProjection`, or `te2AutosaveMode`; do not restore pinned diff projection or the old EOF absorption patch.
- Fact: Normal editor mode still uses custom `te2-draft-del-zone` view zones for draft deletions. Combined diff mode uses Monaco stock deletion widgets tagged with draft styling instead of adding duplicate changed-side layout.
- Fact: Host draft discard uses UI IPC RPC method `ui.host.draft.discard`, handled by `host/draft_backend.py`. Same-file draft-discard reloads must preserve `reason: "discard_external"` so the open path applies disk content instead of taking the navigation fast path.
- Fact: Sidebar draft discovery/clear methods are `sidebar.drafts.list`, `sidebar.draftState.get`, and `sidebar.draft.clear`. Draft list is content-free; draft state returns diff metadata; clear reloads the active editor through the editor-owned reload path when needed.
- Citations: `app/apps/file_editor_cm6/monaco_editor/editor_draft_diff_runtime.ts`, `app/apps/file_editor_cm6/monaco_editor/editor_git_baseline_runtime.ts`, `app/apps/file_editor_cm6/host/draft_backend.py`, `app/apps/file_editor_cm6/host/draft_state_backend.py`, `app/apps/file_editor_cm6/ui_ipc/rpc_contract.py`, `app/apps/file_editor_cm6/ui_ipc/sidebar_rpc_contract.py`, `app/apps/file_editor_cm6/monaco_editor/editor_open_transaction_runner_main.ts`, `worktrees/vscode-te2-diff/src/vs/editor/browser/widget/diffEditor/diffEditorViewModel.ts`

## Agent Edit Review

- Fact: Sidebar draft discovery and inline agent edit review are separate systems. Draft state is TE2/session-cache state; agent edit review is ALS/harness-owned semantic edit state.
- Fact: `/sidebar_ipc` exposes `sidebar.agentEdits.documentState.get`, `sidebar.agentEdits.publish`, `sidebar.agentEdits.clear`, `sidebar.agentEdits.list`, and `sidebar.agentEdits.decide`; `/rpc/editor` exposes editor-facing document-state and decision methods.
- Fact: `host/agent_edit_review_backend.py` is effectively stateless forwarding/normalization for ALS-owned affordances. It emits editor projections and forwards hydration/decision requests to registered sidebar peers; ALS remains canonical for edit session state, accept/reject semantics, restore/revert data, and newer projection publication.
- Fact: Editor file-open triggers ALS agent-edit document-state hydration from the editor-owned `/rpc/editor` lane as the final successful open-transaction step.
- Fact: The vendored chat-editing renderer keys original/deletion zones off the original side (`originalLines` / `originalRange`), not net line-count delta. Equal-size replacements still need an original-zone deletion widget.
- Fact: Older memory about an agent-edit toast string, including `File was chaged...`, is not backed by current source and should be treated as stale unless runtime evidence reappears.
- Citations: `app/apps/file_editor_cm6/host/agent_edit_review_backend.py`, `app/apps/file_editor_cm6/ui_ipc/sidebar_ws.py`, `app/apps/file_editor_cm6/ui_ipc/sidebar_rpc_contract.py`, `app/apps/file_editor_cm6/monaco_editor/editor_agent_edit_review_runtime.ts`, `app/apps/file_editor_cm6/monaco_editor/editor_rpc_contract.py`, `app/apps/file_editor_cm6/monaco_editor/editor_rpc_contract.ts`, `app/apps/file_editor_cm6/monaco_editor/vscode_chat_editing_vendor/chatEditingHunkRenderer.ts`

## Terminal Surfaces

- Fact: The promoted `app/apps/terminal` app has a long-press/right-click shell-kind selector on `New Terminal`. Normal click starts `python`; the menu offers `Python`, `Native`, and `Node`.
- Fact: Terminal app shell creation accepts a simple `kind` field and maps it to `terminal_stream.yaml`, `native_terminal_stream.yaml`, or `node_terminal_stream.yaml`. New shells carry `TERMINAL_STREAM_KIND`; older shells without that env tag remain unlabeled.
- Fact: The Terminal app live contract is JSON-RPC-ish notifications in (`terminal.connect`, `terminal.input`, `terminal.resize`, `terminal.destroy`, `terminal.ping`) and typed terminal-stream frames out (`hello`, `data`, `closed`, `error`, `pong`, `ready`).
- Fact: `file_editor_cm6` terminal drawer source lives at `main_page/frontend/host-terminal-drawer.ts`, uses xterm/FitAddon, loads Android terminal helper scripts, and talks to `terminal_backend.py`.
- Fact: `file_editor_cm6` terminal drawer uses `shellspec/terminal.yaml` with `backend: pty`; earlier dtach duplication conclusions should be considered historical context only.
- Fact: Project sidecars now track multiple terminal shell ids, an active terminal shell id, a shell cap, and optional titles. The legacy single `terminal_shell_id` field is compatibility/migration data.
- Fact: The earlier `terminal_testing` hterm prototype memory is stale in this checkout: `app/apps/terminal_testing` is absent and current planning says xterm replaced the hterm detour.
- Citations: `app/apps/terminal/backend.py`, `app/apps/terminal/src/main.ts`, `app/apps/terminal/manifest.json`, `app/apps/file_editor_cm6/main_page/frontend/host-terminal-drawer.ts`, `app/apps/file_editor_cm6/terminal_backend.py`, `app/apps/file_editor_cm6/project_sidecar.py`, `app/apps/file_editor_cm6/shellspec/terminal.yaml`, `docs/planning/TERMINAL_TESTING_XTERM_MOBILE_PLAN.md`, `docs/planning/TERMINAL_INPUT_LATENCY_PATCH_PLAN.md`

## Android Integration

- Fact: The old Flutter console module is reference-only at `android/fconsole_bridge/`; the live Android console overlay is Compose-based through `ComposeConsoleOverlay.kt`.
- Fact: Android launch should probe the live framework via `/api/android/config` before calling the IPC sleep `/actions/wake` endpoint. Wake idempotence must treat a live registered framework process as already running.
- Fact: GeckoView IME filtering uses `FilteredGeckoView`, `EditorInputFilter`, and `UiIpcClient` focus/blur events to prevent Gboard cursor/composition mismatch.
- Fact: Android generic IME control now listens to generic UI IPC notifications `ui.ime.focus` and `ui.ime.blur`, not only editor-specific focus/blur. This supports terminal surfaces as well as Monaco.
- Fact: Android OTA asset update checks read the server-served `app/apps/file_editor_cm6/static/version.txt` through `/api/editor_version`. Bundled Android assets are a separate publication path under Android assets and should not be conflated with live OTA snapshots.
- Citations: `android/app/src/main/java/com/termux/extensions/ComposeConsoleOverlay.kt`, `android/app/src/main/java/com/termux/extensions/UiIpcClient.kt`, `android/app/src/main/java/com/termux/extensions/EditorAssetManager.kt`, `android/app/src/gecko/java/com/termux/extensions/FilteredGeckoView.kt`, `android/app/src/gecko/java/com/termux/extensions/EditorInputFilter.kt`, `android/app/src/gecko/java/com/termux/extensions/MainActivity.kt`, `android/app/src/webview/java/com/termux/extensions/MainActivity.kt`, `app/main.py`, `app/ipc/server.py`

## Backend Decomposition

- Fact: `app/apps/file_editor_cm6/main.py` still assembles the app worker, but major backend route families moved into `main_page/backend/`: state payload, workbench routes, project routes/service, git routes, and history routes.
- Fact: Extracted FastAPI route factories with strict return annotations that can return `JSONResponse` or `Response` should set `response_model=None`; otherwise FastAPI can try to derive invalid Pydantic response fields from unions.
- Fact: `main_page/backend/project_service.py` is the shared backend project behavior seam used by HTTP project routes and sidebar-facing project wrappers.
- Fact: `host/state_backend.py` owns host file-activity and file-scroll writes through UI IPC RPC methods; the old direct HTTP `/state/file_activity` and `/state/file_scroll` route bodies are removed.
- Citations: `app/apps/file_editor_cm6/main.py`, `app/apps/file_editor_cm6/main_page/backend/state_payload.py`, `app/apps/file_editor_cm6/main_page/backend/workbench_routes.py`, `app/apps/file_editor_cm6/main_page/backend/project_routes.py`, `app/apps/file_editor_cm6/main_page/backend/project_service.py`, `app/apps/file_editor_cm6/main_page/backend/git_routes.py`, `app/apps/file_editor_cm6/main_page/backend/history_routes.py`, `app/apps/file_editor_cm6/host/state_backend.py`, `app/apps/file_editor_cm6/ui_ipc/rpc_dispatch.py`

## FWS, MCP, And Logs

- Fact: Framework-shell log inspection should stay generic and event-first. Do not overfit the tool contract to `codex-app-server` or any single producer.
- Fact: `te2_fws_log_inspect` should surface parser lanes, signatures, and exclusions such as `exclude_query` and `exclude_signature`; long-line inspection should reduce ad hoc Python-heredoc log parsing.
- Fact: `te2_console_eval` treats stale or disconnected worker targets as structured tool-result failures such as `worker_not_registered` or `eval_timeout`, including current live worker lists for retargeting.
- Fact: The root `.rgignore` excludes generated bundles, dependency trees, vendor payloads, Android publication output, archives, backups, worktrees, and TextMate payloads. Search excluded TextMate assets explicitly with `rg --no-ignore` / `rg -uuu` or targeted paths.
- Citations: `docs/planning/FWS_STRUCTURED_LOG_INSPECTION.md`, `docs/planning/FWS_STRUCTURED_LOG_INSPECTION_SUPPLEMENT.md`, `app/te2_mcp/server.py`, `app/te2_mcp/fws_log_analysis.py`, `app/te2_mcp/framework_shells_client.py`, `app/te2_console_runtime.py`, `.rgignore`

## Pruned Or Superseded From Older Memory

- Restored the repo workflow gate as durable repo policy because agents had been bypassing it after `AGENTS.md` was deleted. Detailed workflow policy belongs in `AGENTS.md`; `.repo_memory.md` keeps the concise invariant.
- Dropped most `Version surfaces were synced to 0.2.xxx` slice bullets. Current durable state is the live `0.2.256` sync rule plus current version surfaces.
- Dropped duplicate nested `Repo Memory Snapshot > ...` sections.
- Dropped stale source paths: `src/host`, `static/js`, `m_editor_app.js`, root-level `explorer_rpc_socketio.py`, `services/wba_transport.py`, `app/libs/sidebar_ipc_client.py`, and `app/libs/app_readiness.py`.
- Dropped or rewrote old transport facts: four separate worker Socket.IO servers, sidebar legacy event compatibility, Python WBA relay fallback, `vscode_api`, and `vscode_rpc`.
- Dropped stale terminal-testing hterm prototype facts because `app/apps/terminal_testing` is absent and current planning supersedes hterm with xterm.
- Rewrote older draft/agent-edit notes into the current split: TE2 owns draft discovery/clear and projection; ALS/harness owns semantic agent edit session authority.
