# TE2 Repository Memory

Condensed codebase orientation. Source is authoritative; this file is not a task
tracker, release log, or personal-preference store.
`CODE_TE2.md` below means `docs/apps/code_te2/CODE_TE2.md`; references include
section names and current line numbers. Refresh line references when moving the
manual's sections. Deep contracts and historical provenance belong there.

## Repository Safety And Source Ownership

- Follow `AGENTS.md` for approval, ownership and validation. Preserve unrelated
  edits, drafts and untracked files. Android changes need explicit scope.
- Never restart the shared TE2 runtime without approval. The known Ferrous
  cross-manager defect means `fws down` / `fws terminate` may kill the framework;
  use framework HTTP/Socket lifecycle endpoints for approved worker refreshes.
- Use `$TMPDIR` or a named workspace scratch directory, not hard-coded `/tmp`.
  Search source first; `.rgignore` excludes generated/vendor/worktree noise.
- Nested repositories are independent status/commit scopes. Root pytest excludes
  `worktrees/` to avoid collisions with their own top-level `tests` packages.
- Generated bundles, vendor publication and Android asset copies are outputs,
  not editable source. Validate nested projects from their repository roots.
- Keep this file concise and synchronized with technical documentation; remove
  superseded claims rather than accumulating progress reports or to-do lists.

## Framework Runtime

- Rust under `framework/rust/` is framework authority. `te2` / `te2-rust` enter
  through `app/cli/run_rust_framework.py` and the framework bootstrap, which
  selects the fingerprinted binary and starts the Python console/MCP bridge.
- Python remains for bootstrap/CLI, manifests/scaffolding, console/MCP, pipe
  support and app workers. Do not recreate the retired Python framework,
  supervisor, generic-extension backends, lifecycle or duplicate Git provider.
- Rust owns filesystem/Git/search, settings/state/bookmarks, app lifecycle,
  proxying and shell orchestration. Routine requests do not rediscover FWS
  metadata or scan `/proc`; explicit reconciliation owns discovery.
- Multi-threaded Tokio and bounded per-worker writer queues separate dispatch
  from blocking pipe writes. Queue saturation is explicit, not an event-loop wait.
- `app.libs.jobs` is core-only; no global `/api/jobs` or FastAPI/SSE router.
  Runtime, queue and retained-Python details: `CODE_TE2.md:3943` (§47).

## Framework Assets, Roots And Exposure

- Rust serves shell assets from `app/templates/` and `app/static/`; built-ins
  are in `app/apps/`, user app source in `~/.local/share/te2/apps`.
- Manifests own catalog/readiness contracts. App-shell startup consumes one
  app-scoped `/api/apps/events?app_id=...` SSE stream, not serialized boot GETs.
- `TE2_*_HOME` values are final roots; otherwise XDG plus `/te2` applies.
  `$TE2_DATA_HOME/app_state/<app_id>` is state; `apps/<app_id>` is source.
- Legacy migration is an explicit stopped-framework, dry-run-first command,
  never startup recovery. Published fonts are the four supported WOFF2 faces.
- Loopback is default. `--broadcast` resolves once to bind hosts and exposure
  policy; outer Axum middleware protects HTTP, SSE, raw WS, Socket.IO and proxies.
- IP/CIDR policy checks peers; interface policy checks local destinations.
  Public bind addresses never replace loopback `TE_FRAMEWORK_URL` for workers.
- Exact roots, migration, readiness and network rules:
  `CODE_TE2.md:3943` (§47), `CODE_TE2.md:3992` (Network Exposure).

## Code TE2 Layout And Identity

- Source is `app/apps/code_te2`; `main.py` assembles the app worker,
  `main.ts` boots the host, `monaco_editor/m_editor_app.ts` boots the editor.
- Host UI is `main_page/frontend/`; Explorer is `src/explorer/` plus
  `explorer/`; host actions are `host/`; UI/sidebar IPC is `ui_ipc/`;
  WBA is `workbench_protocol_proxy/`. Source map: `CODE_TE2.md:228` (§1).
- Public id `code_te2` is canonical. `file_editor_cm6` is only a bounded
  request/persisted-state migration alias, never another worker/catalog identity.
- Sidecar recents define bounded logical membership. Stable `clientInstanceId`
  owns one reconnectable foreground with primary/secondary role; `windowId`
  is presentation metadata. `last_file` is only a one-time migration seed.
- Shared close publishes a post-commit `DocumentClosed` fact: affected primaries
  move to MRU, secondaries clear, and exact-client editor state disposes/replaces.
- Frontend paths are projections, never cross-client authority. Foreground,
  shared revisions, drafts and persistence: `CODE_TE2.md:420` (§4).

## RPC And Event Boundaries

- Surfaces use their own lanes: editor `/rpc/editor`, Explorer `/rpc/explorer`,
  host `/ui_ipc`, sidebar `/sidebar_ipc`, drawer `/terminal`.
- Python namespaces share `/api/app/code_te2/socket.io`; direct editor/WBA
  uses `/api/app/code_te2/services/wba/socket.io`. Do not proxy intelligence
  through Python. Console uses `/te2_console_ws/socket.io`, `/te2_console`.
- Editor, Explorer, host and WBA require strict `msgpack-v1`; no JSON fallback.
  Sidebar retains its established codec. Old full socket paths are aliases only.
- Requests emit only when connected; notifications are volatile. Disconnect
  rejects pending requests, clears buffered traffic, and reconnect reconstructs
  state. Do not enable Socket.IO recovery or queue stale editing traffic.
- Cross-surface intent goes frontend -> own backend -> target backend hook ->
  target notification. No private frontend-to-frontend lane shortcuts.
- Codec metadata is default-off via `CODE_TE2_RPC_CODEC_METRICS`. Python
  transport ingress validates `object` mappings rather than propagating Any.
- Transport/codec/ownership contracts: `CODE_TE2.md:188` (§0.6),
  `CODE_TE2.md:538` (§6), `CODE_TE2.md:625` (§6.5).

## Sidebar And Persistence

- Code TE2 data uses canonical data/config/runtime/cache partitions, not legacy
  CM6 roots. Preferences are config; drafts/sidecars are data; sockets runtime.
- Sidebar ledger v2 owns shared membership, stable slot identity and lifecycle;
  host-local presentation owns order, foreground and embedded/hidden/detached.
- Browser presentation is bounded origin/project Local Storage; Electron uses
  validated atomic config IPC; Android uses app-private storage partitioned by
  stable client, upstream origin and project. Random relay ports are not keys.
- Empty initial lists and WBA reset/disconnect snapshots are non-authoritative.
  Only complete authoritative snapshots or explicit removal prune membership.
  Inactive projects remain parked; no polling or grace-window recovery.
- Reconciliation filters by the host's current project. Backend activation replay
  cannot reveal a locally hidden view; explicit local user intent is required.
- Mentions/file opens require exact client, stable host and live presentation
  identity. Re-created hosts re-register; no global-active/broadcast fallback.
- ALS-RS sidebar traffic resolves injected `TE_FRAMEWORK_URL`, then `TE_PORT`,
  then standalone 8089, not its own 12459 listener. Reconnect registers its peer.
- Ownership, storage/migration, focus and exact-client routing:
  `CODE_TE2.md:3796` (§45), `CODE_TE2.md:2990` (§38).

## Search And Explorer Projection

- Rust `service.search` owns file/content search. Python search sessions adapt
  bounded cache/state; the frontend renders progressive events on its own lane.
- Name search projects inside the existing tree, preserves tree DOM/open state,
  expands hit ancestors and shallow-hydrates direct directory matches in one
  bounded `fs.listDirectories` request. No per-hit frontend listing RPCs.
- Backend directory-selection facts update all project clients before local
  dismissal. Sticky search controls retain their bound DOM/IME input.
  Sticky subtree limits use source directory border-box geometry, not estimates.
- Content search retains at most 700 logical hits. Multiple same-line matches
  are separate hits. Supersede, overlay close and project switch cancel sessions.
- Replace targets retained hits, not rendered rows; Show All/Select All expose
  them. Hidden-hit confirmation is separate from draft-discard consent.
- Mobile UA uses long-press/tap selection without checkbox DOM; file headers
  follow group-select/deselect semantics. Desktop uses checkboxes.
- By Changes streams verified selected-tree-to-disk diffs before later candidates
  finish, with no initial full-enumeration gate. Python retains continuation
  metadata only; appends preserve existing diff DOM and ephemeral hunk blinds.
- Limits, edit identities, cancellation, progressive search, sticky projection:
  `CODE_TE2.md:726` (§6.6). Git comparisons: `CODE_TE2.md:1472` (§8).

## Comparison And Guarded Edits

- One Explorer comparison ref drives tree/By Changes metadata, inline commit
  baselines and the far-left status drop-up. HEAD follows current Git facts;
  explicit history stays pinned. Disk mode disables autosave and skips Git reads.
- `GitDiffBaseChanged` owns selection invalidation. Independent latest-only
  projectors schedule reads without blocking the fact bus; matching selection
  revisions deduplicate snapshots without dropping worktree refreshes.
- Historical tree styles compare selected tree against disk; real HEAD/index
  status stays separate for warnings/actions. Actual-state warning icons appear
  only in historical mode and never become filename text.
- Historical stage/commit/reset are rejected by the backend. Restore writes into
  the current worktree, confirms drafts, offers path-scoped unstage and preserves
  the index. Never move HEAD or create a detached worktree for this view.
- Hunk Restore and Find/Replace operate on disk, regardless of autosave.
  Python owns expiring exact-client/project/revision consent and draft safety;
  Rust owns bounded SHA-256/exact-range guarded writes, not draft materialization.
- Reverse hunks come from canonical UTF-8 buffers/libgit2, not display rows.
  Failed/no-op writes retain drafts; newer drafts survive successful operations.
  Accepted mutations survive disconnect; uncertain writes are never retried.
- Guarded apply serializes same-root writes but cannot exclude arbitrary external
  writers between check and rename. Limits and index/draft semantics:
  `CODE_TE2.md:751` (Guarded Text-Edit Foundation), `CODE_TE2.md:828`
  (Search Replacement), `CODE_TE2.md:1472` (comparison/Restore).

## Source Control History

- Explorer History is summary-only: commit/ref graph, filenames and +/- counts.
  File clicks open immutable read-only historical diffs in the second editor.
  It is not WBA-backed and does not place document text in the graph.
- `src/explorer/history/vscode_scm/` contains literal attributed upstream SCM
  copies and pinned base-tree inputs, reproduced through explicit patches.
  Use its UPSTREAM.md, not a lookalike implementation or fake workbench services.
- Rust owns retained metadata revwalks, immutable first-parent file/blob reads,
  and ref/config/reflog watches. Python owns connection-local generations and
  bounded lazy statistics. Strict typed DTOs preserve captured ref/blob identity.
- Native sessions are exact pipe-owner/root/generation scoped. Lease expiry
  notifies once; a visible frontend refreshes once, hidden views defer to events.
  No polling or automatic retry of page advancement/historical file opening.
- Metadata uses pure topological traversal with captured tips seeded newest-first;
  OID ordering or TIME traversal distorts branch continuity. Pinned current,
  upstream and base roles feed unchanged upstream lane colors.
- File statistics are independently bounded from preview admission. Partial totals
  remain partial, not falsely complete; binary/oversized preview states are explicit.
- Frontend generations preserve row DOM, exact file indices, local selection,
  lazy expansion and single-flight early scroll prefetch. Mobile ref/details rows
  and desktop hover use the same statistics projection without extra Git reads.
- Protocols, bounds, watches, upstream provenance and role resolution:
  `CODE_TE2.md:976` (§6.7).

## Historical Secondary Editor

- Backend secondary content state is distinct from working foreground and native
  presentation. File-open tickets are short-lived, bounded and single-use;
  authenticated secondary redemption validates retained project/session identity.
- Working/history kind changes reload only the secondary realm after command
  acknowledgement. Historical revisions abort/replace in place; duplicates reuse.
  Existing atomic persistence needs no additional save/flush, and membership stays.
- Historical boot loads basic/Monarch syntax only and allows the diff worker,
  not language services. Unique `te2-history:` models never enter WBA.
- Read-only controls reject Save/discard/mutation; navigation, selection, Find
  and Copy target the last focused diff side. No working-model fallback.
- Snapshot/live preferences project font/theme/line numbers/wrap through a safe
  allowlist. Theme requests are revision/abort-fenced; stale loads cannot publish.
- Mobile helpers attach to both diff controls and dispose with them. Maintained
  touch fork `historicalReadOnly` allows Copy, Select Word/All, Find and Close,
  ignoring custom editing islands while leaving working tools unchanged.
- Exact lifecycle, handoff, syntax boot and preference/touch integration:
  `CODE_TE2.md:1280` (Secondary History Content State Foundation).

## Documents, Diagnostics And WBA

- Sidecar membership defines logical open documents, not browser model residency.
  WBA retains one role-neutral extension-host document per canonical path and
  one synthetic editor facade per stable client.
- Reconcile protects retained facades plus in-flight opens. Releasing one facade
  cannot release another client's shared document; matching opens are idempotent.
- Admission precedes sidecar mutation: source files including .sh are accepted;
  known binary/media/object/font formats, extensionless executables and disk/draft
  content above 375 KiB are rejected.
- Python materializes only missing/stale background documents. Visible open
  completion is `editor.openComplete.publish`, never merely a WBA acknowledgement.
- Editor intelligence/diagnostics remain direct WBA-to-Monaco. Python diagnostics
  serve Explorer/Problems only; never nudge editor diagnostics from Python.
- Semantic prewarming is bounded retained-document caching with real full
  providers, exact identity and visible-open priority; never manufacture ranges.
- Documents/reconnect: `CODE_TE2.md:3247` (§39); semantic tokens:
  `CODE_TE2.md:2002` (§23); diagnostics: `CODE_TE2.md:2403` (§33).

## Code Server And Extensions

- `webWorkersEnabled` selects mutually exclusive browser-worker and managed
  Code Server modes. Worker mode does not probe WBA; neither mode silently falls
  back. Direct provider dispatch matches all selectors, not language-specific hacks.
- Code Server is pinned/private/confirmation-gated. System/PATH/NVM installs and
  executable overrides are not supported runtime authority.
- Generated managed-runtime paths/config/nids drive WBA. User settings edit the
  private User settings file; Workspace settings edit active .vscode/settings.json.
- Open VSX installs verify artifact identity, allowlisted download origin and
  published SHA-256 before managed local-VSIX install. No server-gallery fallback.
- UI VSIX remains bounded: activity webviews/panels, opaque sandboxing, theme,
  recovery, commands, menus and storage use existing contracts. The legacy
  file-watcher extension API bridge is removed; do not restore it.
- Modes: `CODE_TE2.md:1663` (§11). Installer: `CODE_TE2.md:3412` (§41).
  Extension settings: `CODE_TE2.md:1960` (§22). UI VSIX: `CODE_TE2.md:3641` (§44).

## Editor UI And Mobile Input

- Host menus, tabs and Monaco stacking follow one policy: Monaco host z-index
  auto, widgets at 300; opened host surfaces isolate the editor below them.
- Tabs project open/Git/draft/diagnostic facts. Draft asterisk is structural and
  discard is confirmation-gated; decorations must not reload sidecars off-loop.
- Fresh preferences use draft mode, diff overlays off and vendored github-dark;
  explicit persisted choices override seeds. Product preferences are not agent policy.
- Inline-diff boot starts plain, then baseline application promotes to diff.
  Enabled flags do not prove initialization. Wrap native timers to retain receiver.
- Android Monaco keeps the detached guarded whole-line textarea, coalesced input
  and inputType-aware newline handling. Native EditContext is disabled on Android;
  do not reintroduce raw-key newline suppression or composition-based gating.
- Primary host owns the shared key dock; exact-source/origin secondary presentation
  messages select its focused editor. Terminal focus is a temporary override.
  Floating rails belong to the focused realm, not the primary unconditionally.
- Hold-repeat snapshots target/modifiers once and cancels on release/lifecycle.
  Fullscreen drawer layout preserves status/key rows; Collapse retains secondary
  rendering while Close clears/disposes. Empty/non-terminal drawer opens create no PTY.
- Monaco touch coordinates already include the gutter. Resync layout/content-size
  changes; never subtract stale initialization margins. Read-only mode is fork-owned.
- Touch source/publication: `CODE_TE2.md:2371` (§32); input/key/drawer behavior:
  `CODE_TE2.md:2451` (§35); stacking/comparison: `CODE_TE2.md:1444` (§8).

## Terminals

- Standalone Terminal uses Node/node-pty/headless-xterm; Code TE2's drawer uses
  Python/Pyte. Preserve distinct ownership while sharing input/touch helpers.
- Standalone PTY is raw MessagePack WS; lifecycle/control is websocket-only
  Socket.IO. Node owns checkpoints/sequence/scrollback; shell lists come from
  one FWS snapshot plus events. Active selection is client-local.
- Two-row soft keys compose modifiers, repeat arrows and guard minibar opening.
  Keys/Show exited are local, temporary controls. Dash follows guarded input.
- Drawer control/history uses its existing /terminal Socket.IO lane, not HTTP.
  Pyte parses canonical raw-log bytes off-loop; FWS chunks are wakeups, not text.
- Checkpoints contain ANSI screen state, decoder prefix and exact offset.
  Live binary deltas are offset/generation-fenced; no raw-tail replay, textual
  overlap guesses, decoded fallback, or polling.
- Resize marks Pyte projection stale; next checkpoint reparses at requested
  dimensions. Do not resize retained screens or replay on routine viewport changes.
- Shared Android xterm cumulative input is printable-text authority; keypress
  fallback is bypassed, modified/non-text keydown remains authoritative, and
  guard repair plus the source input hook handle stuck Gboard/229 behavior.
- Xterm source owns the transparent scroll surface and irreversible gesture
  classification; the mobile adapter owns anchored selection, handles and menu.
  No synthetic mouse lifecycle for handle drag; completed taps alone replay it.
- Standalone controls: `CODE_TE2.md:4146` (§48); drawer: `CODE_TE2.md:3875`
  (§46); shared input/touch source, publication and offset rules: §35 at line 2451.

## Native Clients

- GeckoView is primary Android; Cefrium is isolated, not a system-WebView fallback.
  Android settings and stable identities are application-private per APK.
- Android assets come only from OTA/APK materialization; declared missing files
  fail closed. Block TE2 /sw.js and use scoped cleanup, not a blanket SW ban.
- PersistentNetworkService owns relay/routes/UI IPC/console across activity
  recreation. Retargeting runs through configure; activities do not duplicate it.
- Route projection and inspector targets are separate. Cold restore needs a fresh
  authoritative route snapshot, not persisted routes. No native presentation polling.
- Gecko WebExtension and Cefrium native-query bridges carry stable client/secondary
  identity and presentation state. Reset rotates primary/secondary atomically.
- Cefrium's native multi-touch/double-tap zoom controls are not complete zoom
  suppression. Keep the renderer-scoped 16px Monaco IME/Find input floor.
- IME context switching is opt-in. Permission/power refresh is foreground-event
  driven, and both activities resynchronize the persistent service on resume.
- Electron owns its trusted shell origin, framework WebContentsView, loopback relay,
  validated IPC and stable distinct editor identities; launch uses --no-sandbox.
- Electron bootstrap reconnects its existing native UI IPC client when the worker
  becomes ready. An early namespace refusal must not strand route readiness.
- Electron connection/bookmark selection is an immediate main-process transaction.
  Local-process ownership and immutable launch config are distinct from edits to
  next-launch settings; opt-in autostart never starts a missing framework implicitly.
- Android assets: `CODE_TE2.md:2602` (§36); Electron/secondary: §38 at line 2990;
  Cefrium: §42 at line 3467; Gecko inspector: §43 at line 3554.

## Console And Shared UI

- TE2 console is frontend runtime observability, not shell stdio. Discover exact
  live worker IDs before eval; labels can represent multiple windows/clients.
- `te2 console list-workers/eval` require a live framework; tail/search read
  retained transcript. Bounded replay and volatile telemetry avoid socket backlogs.
- Framework-Shell logs are separate; inspect structured metadata before raw logs.
  Raw historical lines have no invented per-line timestamps; stdin is opt-in.
- Async teUI dialogs replace browser prompts; Electron uses same-origin portal
  child windows. Popup listboxes belong to the dialog layer, not clipped bodies.
- Dependency-free component-runtime owns synchronous JSX/refs/disposal, not state
  authority or a VDOM. Modal-kit re-exports its JSX runtime; keep CM6 JSON fields.
- Console: `CODE_TE2.md:2129` (§25); host/UI source: §1 at line 228;
  FWS execution/transport distinction: §0.5 at line 149.

## Build And Publication

- Frontend validation/publication: run npm run typecheck and node build.mjs in
  app/apps/code_te2. Host output is static/dist/host.js; do not revive editor.js.
- Native contract handoffs synchronize release-facing versions and build frontend
  before approved Android asset bundling/APK builds. Never seed stale assets.
- Keep Cargo caches. Framework builds are optimized unless --debug; publication
  validates a selected fingerprinted binary under the shared build lock.
- Installed releases use verified native artifacts with explicit provenance and
  --build-info contracts; binary installs cannot silently fall back to Cargo.
- Linux uses private pinned Node/npm/headers from nodejs-wheel; Termux uses its
  declared apt runtime. Node-pty needs build-essential on supported Linux installs.
- Dependency locks, native ABI tags, FastMCP compatibility, clean wheel staging,
  Termux/Linux installation and current-plus-one rollback are documented in
  `CODE_TE2.md:4008` (§47 Build And Installation Contracts).
- Historical checksums/tags/acceptance belong in `CODE_TE2.md:4190` (§49), not
  always-loaded memory. Treat them as publication-time evidence, not current status.
