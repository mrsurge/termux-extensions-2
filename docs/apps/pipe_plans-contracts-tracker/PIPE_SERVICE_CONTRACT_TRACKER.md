# Pipe Service Contract Tracker

Status values:

- `todo`: not started
- `doing`: actively being worked
- `done`: completed and accepted
- `blocked`: waiting on a specific dependency
- `deferred`: intentionally out of current phase

## Contract Work

| Status | Item                                                                        | Outcome                                                                                                                              |
| ------ | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| done   | Create pipe service planning directory                                      | `docs/apps/pipe_plans-contracts-tracker` exists.                                                                                     |
| done   | Move old pygit2/event-bridge plan into the new directory without reading it | Moved for later intake.                                                                                                              |
| done   | Draft contract-first migration plan                                         | `FILE_EDITOR_CM6_PIPE_SERVICE_MIGRATION_PLAN.md` created.                                                                            |
| done   | Draft JSON-RPC pipe contract                                                | `PIPE_SERVICE_JSON_RPC_CONTRACT_DRAFT.md` created.                                                                                   |
| done   | Review old plan and merge only still-relevant durable facts                 | Ordering, replay, store authority, stdout conflict, correlation ids, WBA boundary, and git cleanup were merged into the active docs. |
| done   | Mark old runtime-control-plane plan as superseded                           | Old file is retained as historical context under this directory.                                                                     |
| todo   | Decide whether NIDs are fully runtime-assigned or partially static          | Contract currently supports both with runtime assignment preferred.                                                                  |
| done   | Decide first service family for implementation                              | First slice is Code TE2 Explorer read-only directory listing DTO adoption.                                                           |
| todo   | Decide location for shared DTO schema source                                | Needs Python/Rust/TypeScript generation strategy.                                                                                    |
| todo   | Define stdout metrics/log migration for protocol-mode workers               | Old event-bus stdout metrics conflict with stdout-as-protocol.                                                                       |
| todo   | Define correlation id propagation for project switch and WBA reconnect      | Contract has `correlationId`; source wiring remains future work.                                                                     |
| done   | Add Code TE2 Explorer FS service-client seam                                | `explorer/services/fs_service_client.py` owns listing origin selection and defaults to in-process DTO production.                    |

## Cutover Sprint Plan

| Status | Sprint           | Scope                                                                                                                             | Outcome                                                                                                                                                                                                                                                                                                                                                                                       |
| ------ | ---------------- | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| done   | Explorer slice 1 | Cut over Explorer Git status/decorations plus synchronous `service.git` request/response operations already covered by Rust DTOs. | Explorer now uses pipe-backed wrappers for stage/unstage/stage-all/unstage-all/basic commit/init/branch-list/history-list.                                                                                                                                                                                                                                                                    |
| done   | Explorer slice 2 | Cut over Explorer long-running Git jobs and remaining Explorer-specific Git provider gaps.                                        | Clone/pull/push start through `git.clone.start`, `git.pull.start`, and `git.push.start`; routed `git.job.progress` is translated to existing `explorer.job.progress`. Explorer restore/reset/diff-base commit metadata/search changed-file discovery/name-search index now use `service.git` pipe adapters. Backend cancel reaches `git.job.cancel`; cancel UI remains a later frontend task. |
| done   | Main UI slice 1  | Cut over host/main-page Git routes and host state payload consumers.                                                              | Main UI Git route dependencies use pipe-backed DTO consumers while preserving existing HTTP/UI behavior.                                                                                                                                                                                                                                                                                      |
| done   | Editor Git phase | Editor baseline/diff/open-adjacent Git consumers.                                                                                 | Editor document open/save stayed direct; editor Git baseline/diff producers now use `service.git` pipe DTOs.                                                                                                                                                                                                                                                                                  |

## Future Consumer Audit

| Status | Consumer                        | Needed DTOs                                                                    | Outcome                                                                                                                                                                                      |
| ------ | ------------------------------- | ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| done   | Explorer listing/tree           | `FsDirectoryListing`, `FsMutationResult`, `FileChangeBatch`                    | Listing plus create file/directory, create project, rename, delete, copy, move, copy-from, and move-from route through `service.fs`; no local FS fallback.                                   |
| done   | Explorer git decorations/status | `GitSnapshot`                                                                  | Explorer status/decorations consume pipe-backed `GitSnapshot` through `worker_services/git_service.py`.                                                                                      |
| done   | Explorer search                 | `SearchFilesResult`, `SearchContentResult`; progressive `SearchProgress` later | File-name and content search now consume Rust `service.search` request/response DTOs through `search.files.get` and `search.content.get`; progressive search jobs/cancel remain future work. |
| todo   | Editor open/read                | `FsReadResult`, `FsStatResult`                                                 | Preserve current open transaction semantics.                                                                                                                                                 |
| todo   | Editor save/write               | `FsWriteResult`, `FileChangeBatch`                                             | Preserve atomic write behavior.                                                                                                                                                              |
| done   | Editor git baselines            | `GitHeadBlobResult`, `GitDiffHunks`                                            | Editor baseline reads use `git.headBlob`, and commit/diff decoration producers use `git.diff.hunks`; document open/save stays direct.                                                        |
| done   | Main host git UI                | `GitBranchList`, `GitRemoteList`, `GitMutationResult`                          | UI IPC stays the host lane; branch menu and HTTP main git routes consume pipe-backed worker git service adapters.                                                                            |
| todo   | WBA watcher bridge              | `FileChangeBatch`                                                              | Normalize watcher input only; do not move language intelligence.                                                                                                                             |
| todo   | Python framework router         | protocol envelopes, stderr/stdout supervision                                  | Own first service lifecycle implementation.                                                                                                                                                  |
| done   | Rust spike router               | same protocol envelopes                                                        | Rust spike consumes the same envelope contract for `fs.listDirectory`, Explorer FS mutations, and framework-owned `service.git` methods.                                                     |
| todo   | Store-backed replay consumers   | replay metadata plus existing store authority                                  | Preferences/history/sidecars/open-state/sidebar ledger remain authoritative for now.                                                                                                         |

## Producer DTO Generation

| Status | Producer                      | Current Source                                                                                                         | Outcome                                                                                                                                                                                                                                                              |
| ------ | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| done   | Explorer directory listing    | `explorer/services/fs_service_client.py`, Rust `service.fs`                                                            | Listing calls `fs.listDirectory` over the pipe and adapts `FsDirectoryListing` to the current Explorer payload.                                                                                                                                                      |
| done   | Explorer file mutations       | `explorer/services/fs_service_client.py`, `explorer/services/file_ops.py`, Rust `service.fs`                           | Explorer create file/directory, create project, rename, delete, copy, move, copy-from, and move-from consume `FsMutationResult` over the pipe.                                                                                                                       |
| done   | Git snapshot for Explorer     | `worker_services/git_service.py`, `explorer/services/runtime_notifications.py`, `explorer/services/file_ops.py`        | In-process git now emits one `GitSnapshot` DTO and projects it to existing status/decorations notifications.                                                                                                                                                         |
| done   | Rust framework Git snapshot   | `framework_services/git_ops.rs`, `framework_services/pipe/git_pipe_ops.rs`                                             | Rust framework uses `git2`/libgit2 to generate the contract `GitSnapshot` DTO for `git.snapshot.get`; no `git` subprocess path.                                                                                                                                      |
| done   | Git mutations/history/remotes | `worker_services/git_service.py`, `explorer/handlers/git.py`, `host/git_backend.py`, `main_page/backend/git_routes.py` | Explorer and main UI adopt pipe-backed `GitMutationResult`, `GitBranchList`, `GitRemoteList`, `GitHistoryResult`, `GitJobStarted`, `GitJobCancelResult`, `GitCommitInfoResult`, `GitWorktreeChanges`, `GitPathIndex`, reset hard, and restore where Rust DTOs exist. |
| done   | Git diff                      | `diff_helper.py`, `worker_services/git_service.py`                                                                     | `diff_helper.collect_diff` keeps the old frontend payload shape while sourcing hunks from `git.diff.hunks`.                                                                                                                                                          |
| done   | Dead Git producer cleanup     | `git_helper.py`                                                                                                        | Removed the old subprocess-backed Git helper after live Python imports moved to `worker_services/git_service.py`.                                                                                                                                                    |
| done   | Dead FS producer cleanup      | `main.py`, `explorer/services/file_ops.py`                                                                             | Removed the app-worker `fs.listDirectory` producer and local `os.scandir` listing builder; `file_ops.py` only adapts/delegates pipe DTOs for Explorer.                                                                                                               |
| done   | Explorer search               | `explorer/search.py`, Rust `service.search`                                                                            | File-name and file-content search overlay data route through explicit `SearchFilesResult` / `SearchContentResult` pipe DTOs; progress/cancel remains a future extension.                                                                                             |
| todo   | Editor disk reads             | `monaco_editor/editor_ws.py`, `core_read.py`                                                                           | Emit `FsReadResult`.                                                                                                                                                                                                                                                 |
| todo   | Editor disk writes            | `core_write.py`, save services                                                                                         | Emit `FsWriteResult`.                                                                                                                                                                                                                                                |
| todo   | Editor baselines              | `editor_view_state_backend.py`                                                                                         | Emit `EditorBaselineSnapshot`.                                                                                                                                                                                                                                       |
| todo   | Watcher input                 | `wba_event_bridge.py`, `workspace_events.py`, `watchexec_shell_manager.py`                                             | Emit `FileChangeBatch`.                                                                                                                                                                                                                                              |
| todo   | Workspace side-effect audit   | `workspace_events.py`                                                                                                  | Classify existing side effects as DTO producers, invalidators, or lane projectors before pipe cutover.                                                                                                                                                               |

## Consumer DTO Acceptance

| Status | Consumer                         | Current Source                                                       | Outcome                                                                                                                                                                                                         |
| ------ | -------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| done   | Explorer list projection         | `explorer/services/render_state.py`                                  | Render-state consumes `FsDirectoryListing` through `fs_service_client` and emits existing `explorer.list.updated` payloads.                                                                                     |
| done   | Explorer git projection          | `explorer/services/render_state.py`, runtime notifications           | Status/decorations consume `GitSnapshot`; Explorer Git commands/search and pipe job progress are adapted through `worker_services/git_service.py` and `job_tracking.py` without direct `git_helper.py` imports. |
| done   | Explorer search frontend/backend | `explorer/handlers/search.py`, `explorer/contracts/search_review.py` | Backend accepts `service.search` DTOs and projects them onto the existing Explorer frontend lane; no frontend search contract change required.                                                                  |
| todo   | Editor open path                 | editor open services and frontend open transaction                   | Accept read/open DTOs without changing visible behavior.                                                                                                                                                        |
| todo   | Editor save path                 | editor save services                                                 | Accept write DTOs.                                                                                                                                                                                              |
| done   | Editor baseline path             | editor view-state/git baseline runtime                               | Accepts `GitHeadBlobResult`/`GitDiffHunks` through `worker_services/git_service.py`; document open/save paths remain direct.                                                                                    |
| done   | Host git branch/remotes          | `host/git_backend.py`, host frontend git menu                        | Host branch menu uses UI IPC and pipe-backed worker git service adapters for branch list/checkout/create and remote add/origin.                                                                                 |
| todo   | Bootstrap/replay projector       | backend projection layer                                             | Enforce active project, readiness, open-state, tree, git, watcher, diagnostics, draft/review, and preferences/sidebar replay order.                                                                             |

## Codec And Runtime

| Status | Item                                  | Outcome                                                                                                                                                    |
| ------ | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| todo   | Implement NDJSON JSON-RPC codec       | stdin/stdout protocol-only baseline.                                                                                                                       |
| todo   | Add stdout guard for service shells   | Accidental stdout must be treated as protocol corruption.                                                                                                  |
| doing  | Route by request id                   | Rust bridge validates one response id; broader concurrent routing table remains open.                                                                      |
| doing  | Route by app id and target identity   | Rust bridge path selects app; `targetName`/`targetNid` select and validate the app-internal service.                                                       |
| doing  | Add progress/cancel op handling       | Git clone/pull/push progress notifications are routed into Explorer; backend cancel RPC reaches `git.job.cancel`, while UI cancel controls remain pending. |
| todo   | Add stderr capture and tagging        | Human logs must stay off stdout.                                                                                                                           |
| todo   | Move protocol-mode metrics off stdout | Use stderr, framework log sink, or explicit protocol notifications.                                                                                        |
| todo   | Add correlation id propagation        | Project switch, WBA reconnect, open-file replay, search, git jobs, and recursive fs operations.                                                            |
| todo   | Add fake service conformance tests    | Validate protocol before real service cutover.                                                                                                             |

## Pipe Cutover

| Status | Service Family                                         | Outcome                                                                                                                                                                                      |
| ------ | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| done   | Explorer fs list/mutations                             | Code TE2 app-worker is pipe-backed for `service.fs`; Explorer listing and tree mutations are pipe-or-error with no local fallback. Stat/read DTOs remain pending for non-Explorer consumers. |
| done   | Git snapshot, sync Explorer Git, and Explorer Git jobs | Code TE2 Explorer consumes `service.git` for snapshot/status, sync Git commands, clone/pull/push job starts/progress, reset, commit info, worktree changes, and path-index search.           |
| done   | Editor Git baseline/diff                               | Uses `git.headBlob` and `git.diff.hunks`; editor document open/save stays off pipe.                                                                                                          |
| done   | Rust search service                                    | File-name and file-content search overlay DTOs are behind Rust `service.search` request/response methods; progressive progress/cancel is not part of this first cutover.                     |
| todo   | Search truncation semantics                            | Remove implicit per-file match caps from Rust `service.search`, or add explicit per-file/global materialization affordances before any such cap is applied.                                  |
| done   | Explorer FS mutations                                  | Explorer tree mutations use `FsMutationResult` from `service.fs`; editor save/write remains intentionally out of scope.                                                                      |
| done   | Git mutations/history/remotes                          | Explorer and main host git UI consume pipe-backed sync mutation/history/remote DTOs; network job progress remains on the Explorer job path.                                                  |
| todo   | OS helpers                                             | Later service family.                                                                                                                                                                        |

## Decisions

| Status   | Decision                                                      | Outcome                                                                                                            |
| -------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| done     | Contract before transport                                     | Existing producers/consumers adopt DTOs before stdout pipe cutover.                                                |
| done     | stdout reserved for protocol                                  | All logs go to stderr.                                                                                             |
| done     | Request id and NID are separate                               | `id` routes response; NID routes participant lanes.                                                                |
| done     | Use `projectGeneration` for stale rejection                   | Prevents project-switch stale results from applying.                                                               |
| done     | WBA language intelligence stays out of this pipe              | Only watcher/file-change normalization is in scope.                                                                |
| done     | Old event-bus tracker is historical                           | Do not carry old phases forward as active architecture work.                                                       |
| done     | GitPython/pygit2/git2 are implementation details              | The active target is the `service.git` pipe boundary; Rust uses `git2`/libgit2 for the current framework provider. |
| done     | Store authority remains in existing app stores for this phase | Pipe services return operation DTOs; they do not own app preference/history/sidecar truth yet.                     |
| deferred | Full persistence service for sidecars/preferences/history     | Not part of initial fs/git/os pipe cutover.                                                                        |

## Deprecated Or Redirected Old-Plan Items

| Status     | Old Item                                         | New Direction                                                                           |
| ---------- | ------------------------------------------------ | --------------------------------------------------------------------------------------- |
| deprecated | stdout event-bus metrics as the metrics sink     | Invalid for protocol-mode stdout; move to stderr/framework logs/protocol notifications. |
| redirected | GitPython-backed unified git service             | Replace with `service.git` pipe boundary; implementation library remains swappable.     |
| deprecated | Event bus as the central low-level work executor | Keep any bus/control-plane role separate from blocking fs/git/os execution.             |
| retained   | Project generation stale drops                   | Required on all project-scoped service DTOs.                                            |
| retained   | Surface projection lane rules                    | DTO consumers project onto their own existing lanes only.                               |
| retained   | Bootstrap/replay ordering                        | Must be enforced by app/framework projectors after service responses.                   |
