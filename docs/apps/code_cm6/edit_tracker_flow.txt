┌─────────────────────────────────────────────────────────────────────────────┐
│                         EDIT TRACKER DATA FLOW                              │
└─────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
 PHASE 1: SHELL ACTIVATION
═══════════════════════════════════════════════════════════════════════════════

   User Opens Terminal                 User Opens Agent Drawer
           ↓                                      ↓
   Terminal WebSocket Connect          Agent WebSocket Connect
   (/ws/terminal/shell_123)           (/ws/agent?agent=codex)
           ↓                                      ↓
   terminal_backend.py                 agent_ws.py
   Line 184: register_shell_watcher   Line 213: register_shell_watcher
           ↓                                      ↓
           └──────────────┬───────────────────────┘
                          ↓
              edit_tracker.register_shell_watcher(shell_id, type)
                          ↓
              _active_shells[shell_id] = type
                          ↓
              _emit_status() → WebSocket event
                          ↓
              ┌───────────────────────────────────┐
              │ Event: "tracking_status"           │
              │ { active: true, shells: [...] }    │
              └───────────────────────────────────┘
                          ↓
              Frontend: handleEditTrackerEvent()
                          ↓
              updateEditTrackerStatus()
                          ↓
              Status Bar: "🤖 Tracking (1 terminal)"


═══════════════════════════════════════════════════════════════════════════════
 PHASE 2: FILE MODIFICATION
═══════════════════════════════════════════════════════════════════════════════

   Agent Modifies File
   (e.g., Codex writes to main.py)
           ↓
   Filesystem Change Detected
   (watchdog or polling)
           ↓
   core_read.py: _handle_fs_event()
           ↓
   core_read.py: _do_handle_fs_event()
   Line 244: edit_tracker.on_file_modified(path)
           ↓
   edit_tracker.on_file_modified(path)
           ↓
   ┌─────────────────────────────────────────┐
   │ 1. Convert to relative path             │
   │ 2. Call diff_helper.collect_diff()      │
   │ 3. Parse hunks for line numbers         │
   │ 4. Build edit_data dict                 │
   └─────────────────────────────────────────┘
           ↓
   Git Diff Execution:
   $ git diff --unified=0 -- main.py
           ↓
   Parse Hunks:
   @@ -10,2 +10,3 @@        →  newStart = 10
   +added line                   newLines = 3
   +another line                 oldStart = 10
                                 oldLines = 2
           ↓
   Extract First Modified Line:
   first_line = 10
           ↓
   edit_data = {
     path: "/project/main.py",
     rel_path: "main.py",
     line: 10,
     timestamp: 1698765432.123,
     hunks_count: 1,
     added: 3,
     deleted: 2
   }
           ↓
   _emit_edit(edit_data) → WebSocket event
           ↓
   ┌───────────────────────────────────┐
   │ Event: "edit_tracked"              │
   │ { path: ..., line: 10, ... }       │
   └───────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
 PHASE 3: AUTO-JUMP
═══════════════════════════════════════════════════════════════════════════════

   Frontend: handleEditTrackerEvent()
           ↓
   Check: trackAgentEdits === true ?
           ↓ YES
   autoJumpToEdit(path, line)
           ↓
   ┌─────────────────────────────────────────┐
   │ 1. Check if file already open           │
   │ 2. If not: await openFile(path)         │
   │ 3. Wait 50ms for editor ready           │
   │ 4. Get line object from doc             │
   │ 5. Calculate position (lineObj.from)    │
   └─────────────────────────────────────────┘
           ↓
   editorView.dispatch({
     selection: { anchor: pos },
     scrollIntoView: true
   })
           ↓
   flashLine(lineNumber)
           ↓
   ┌─────────────────────────────────────────┐
   │ 1. Create line decoration                │
   │ 2. Add .cm-edit-flash class             │
   │ 3. Apply yellow highlight                │
   │ 4. setTimeout(1000ms) → remove          │
   └─────────────────────────────────────────┘
           ↓
   ┌─────────────────────────────────────────┐
   │ VISUAL RESULT:                           │
   │ • Editor scrolls to line 10              │
   │ • Line 10 flashes yellow                 │
   │ • Highlight fades over 1 second          │
   │ • User sees exactly what agent changed   │
   └─────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
 PHASE 4: SHELL CLEANUP
═══════════════════════════════════════════════════════════════════════════════

   User Closes Terminal/Agent
           ↓
   WebSocket Disconnect
           ↓
   terminal_backend.py / agent_ws.py
   finally block: unregister_shell_watcher(shell_id)
           ↓
   edit_tracker.unregister_shell_watcher(shell_id)
           ↓
   _active_shells.pop(shell_id)
           ↓
   If no shells remain:
     _last_edit = None
           ↓
   _emit_status() → WebSocket event
           ↓
   ┌───────────────────────────────────┐
   │ Event: "tracking_status"           │
   │ { active: false, shells: [] }      │
   └───────────────────────────────────┘
           ↓
   Frontend: updateEditTrackerStatus()
           ↓
   Status Bar: Hidden (display: none)


═══════════════════════════════════════════════════════════════════════════════
 KEY INTEGRATION POINTS
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────┐
│ terminal_backend.py │  ──┐
└─────────────────────┘    │
                           ├──→ register_shell_watcher()
┌─────────────────────┐    │
│ agent_ws.py         │  ──┘
└─────────────────────┘

┌─────────────────────┐
│ core_read.py        │  ────→ on_file_modified()
└─────────────────────┘

┌─────────────────────┐
│ diff_helper.py      │  ────→ collect_diff() [used by edit_tracker]
└─────────────────────┘

┌─────────────────────┐
│ main.py             │  ────→ REST + WebSocket endpoints
└─────────────────────┘

┌─────────────────────┐
│ main.js             │  ────→ Frontend UI + auto-jump logic
└─────────────────────┘

