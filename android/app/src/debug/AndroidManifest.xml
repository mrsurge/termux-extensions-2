<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- ADB shell has DUMP; ordinary applications cannot invoke this receiver. -->
        <receiver android:name=".NativeDebugReceiver" android:exported="true"
            android:permission="android.permission.DUMP" />
    </application>
</manifest>
