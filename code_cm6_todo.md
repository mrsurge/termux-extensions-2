# code_cm6 — Active TODO List

- [x] **Open Files tab (Recent History) wiring** – Reconnect the “Open Files” dropdown to the disk-backed history store. Reuse the `/state/file_activity` payloads to hydrate the menu on load, attach a click handler that delegates to `window.appOpenFileAbs/Rel`, and emit “recent-list-changed” events whenever recents mutate so the drawer and the dropdown stay in sync.
- [x] **On-disk preference saving** – Persist Theme/View/Autosave toggles by flowing them through the existing preferences helper (`preferences_store.py`). Ensure the frontend posts the updated state whenever a toggle changes and that worker boot replays those values into CodeMirror extensions before rendering.
- [x] **Drawer Git/filetype styling** – Extend the explorer payloads with cached git status metadata and executable flags sourced from the backend explorer helper. The drawer now renders directories in bold blue, executables in bold green, plain files in white, and tint markers along the gutter for modified/untracked/staged states.
- [ ] **Git diffs in editor** – Surface inline gutter/inline diff decorations by exposing a lightweight `/diff` endpoint that streams `git diff --unified=0` hunks. On the frontend, debounce fetches when a document becomes dirty, cache the decorations per path, and give users a View-menu toggle to disable diff overlays on low-power devices.
- [ ] **Utility drawer (terminal/agent shelf)** – Embed a collapsible right-side drawer that can host the terminal bridge and future agent output. Reuse the existing framework terminal WebSocket/channel, mirror session lifecycle events into the drawer, and provide an API so other apps can contribute panels.
- [ ] **Framework-wide WebSocket bus** – Design a shared `ws_bus` module (Python + JS) that multiplexes per-app channels over a single worker connection. Define a subscription API, ensure the supervisor still handles per-app auth, and document handshake conventions so apps like code_cm6 can opt into the bus for diffs, git status, or collaboration messages.
